# Plan 08: 릴릭 시스템

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 10종 릴릭의 효과를 게임플레이에 통합한다. 보스 처치 보상, 이벤트, 상점에서 획득. BattleManager와 TeamManager에 릴릭 트리거 훅 추가. MapScene/BattleScene에 릭 UI 표시.

**Architecture:**
- `RelicResource.trigger: TriggerType` 추가 — 언제 효과가 발동하는지 정의
- `GameManager.relics` 배열에 릴릭 저장
- BattleManager 주요 이벤트(턴 시작, 피해, 전투 시작 등)에서 `GameManager.trigger_relics(trigger)` 호출
- 릴릭 효과는 GameManager에서 처리 (BattleManager 의존성 최소화)

**Tech Stack:** Godot 4.6, GDScript, 헤드리스 테스트 (기존 ~97개 + 신규 ~8개 → ~105개)

---

## 브랜치

```bash
git checkout -b feat/plan-08-relics
```

---

## 릴릭 10종 정의

### 공용 릴릭 (7종)

| 이름 | 트리거 | 효과 |
|------|--------|------|
| **버닝 블러드** | 전투 승리 | HP +6 회복 |
| **불사조 깃털** | 플레이어 턴 시작 | 에너지 +1 (첫 턴만) |
| **독약 병** | 전투 시작 | 무작위 적에게 독 3 |
| **전쟁 북** | 플레이어 턴 시작 | 카드 +1 드로우 |
| **고대 유물** | 패시브 | 최대 HP +15 (런 시작 시 적용) |
| **모래시계** | 플레이어 턴 종료 | 버린 카드 중 1장을 드로우 더미 맨 위에 |
| **피의 돌** | 영웅 피해 시 | 피해량 ≥ 5이면 에너지 +1 |

### 캐릭터 전용 릴릭 (3종)

| 이름 | 캐릭터 | 효과 |
|------|--------|------|
| **황제의 인장** | 나폴레옹 | 전투 시작 시 사기 +2. 나폴레옹 생존 시 CONSUME_MORALE 카드 비용 -1 |
| **독사의 팔찌** | 클레오파트라 | 전투 시작 시 무작위 적 독 2. 클레오파트라 생존 시 독 4 |
| **거북선 모형** | 이순신 | 플레이어 턴 시작 시 BLOCK +2. 이순신 생존 시 BLOCK +4 |

---

## 파일 구조

```
resources/
  relic_resource.gd     ← (수정) TriggerType 추가
autoload/
  game_manager.gd       ← (수정) relics + trigger_relics() + 릴릭 보상 처리
  battle_manager.gd     ← (수정) 릴릭 트리거 훅 추가
scenes/
  map/map_scene.gd      ← (수정) 릴릭 UI 표시
  battle/battle_scene.gd ← (수정) 릴릭 UI 표시
tests/
  test_relics.gd        ← (신규)
  test_runner.gd        ← (수정)
```

---

## Task 1: RelicResource 확장

- [ ] **Step 1: relic_resource.gd에 TriggerType 추가**

```gdscript
# resources/relic_resource.gd
class_name RelicResource
extends Resource

enum TriggerType {
    PASSIVE,           # 런 시작 시 1회 적용 (최대 HP 등)
    BATTLE_START,      # 전투 시작
    PLAYER_TURN_START, # 플레이어 턴 시작
    PLAYER_TURN_END,   # 플레이어 턴 종료
    BATTLE_WIN,        # 전투 승리
    ON_HERO_DAMAGED,   # 영웅 피해 시
}

enum EffectType {
    HEAL, ENERGY, DRAW, APPLY_STATUS_ENEMY, MAX_HP, RECOVER_CARD,
    GAIN_MORALE, COST_REDUCTION, BLOCK
}

@export var relic_name: String = ""
@export var description: String = ""
@export var trigger: TriggerType = TriggerType.PASSIVE
@export var effect_type: EffectType = EffectType.HEAL
@export var value: int = 0
@export var owner_hero_id: String = ""  # "" = 공용. 특정 id = 해당 캐릭터 전용
@export var bonus_value: int = 0        # 전용 캐릭터 생존 시 value 대신 사용
@export var condition_value: int = 0    # ON_HERO_DAMAGED: 최소 피해량 조건 등
```

---

## Task 2: GameManager — 릴릭 풀 + trigger_relics()

- [ ] **Step 1: GameManager에 릴릭 빌더 추가**

```gdscript
func _build_relic_pool() -> Array:
    var RelicRes = load("res://resources/relic_resource.gd")
    var relics := []
    # 버닝 블러드
    var r1 := RelicRes.new(); r1.relic_name = "버닝 블러드"
    r1.trigger = RelicRes.TriggerType.BATTLE_WIN
    r1.effect_type = RelicRes.EffectType.HEAL; r1.value = 6
    relics.append(r1)
    # ... (10종 모두)
    return relics

func get_random_relic() -> Resource:
    var pool := _build_relic_pool()
    # 이미 보유한 릴릭 제외
    var owned_names := relics.map(func(r): return r.relic_name)
    var available := pool.filter(func(r): return r.relic_name not in owned_names)
    if available.is_empty(): return null
    return available[randi() % available.size()]
```

- [ ] **Step 2: trigger_relics(trigger: int) 구현**

```gdscript
func trigger_relics(trigger: int, context: Dictionary = {}) -> void:
    for relic in relics:
        if relic.trigger != trigger:
            continue
        # 전용 캐릭터 체크
        var effective_value: int = relic.value
        if relic.owner_hero_id != "":
            if not _is_hero_alive(relic.owner_hero_id):
                continue  # 전용 캐릭터 사망 시 효과 없음
            effective_value = relic.bonus_value  # 생존 시 강화 효과
        _apply_relic_effect(relic, effective_value, context)

func _is_hero_alive(hero_id: String) -> bool:
    if not is_inside_tree(): return false
    return TeamManager.is_alive(hero_id)

func _apply_relic_effect(relic: Resource, value: int, context: Dictionary) -> void:
    var RelicRes = load("res://resources/relic_resource.gd")
    match relic.effect_type:
        RelicRes.EffectType.HEAL:
            if is_inside_tree():
                for hero in TeamManager.heroes:
                    TeamManager.heal(hero.hero_id, value)
        RelicRes.EffectType.ENERGY:
            if is_inside_tree() and DeckManager:
                DeckManager.current_energy += value
                DeckManager.energy_changed.emit(DeckManager.current_energy)
        RelicRes.EffectType.DRAW:
            if is_inside_tree() and DeckManager:
                DeckManager.draw_cards(value)
        RelicEs.EffectType.BLOCK:
            # BattleManager에게 위임 (시그널 또는 직접 참조)
            if is_inside_tree() and BattleManager:
                for hero in TeamManager.heroes:
                    BattleManager._hero_block[hero.hero_id] = \
                        BattleManager._hero_block.get(hero.hero_id, 0) + value
        RelicRes.EffectType.APPLY_STATUS_ENEMY:
            if is_inside_tree() and BattleManager and BattleManager.is_battle_active:
                for i in range(BattleManager._enemies.size()):
                    if BattleManager._enemy_alive[i]:
                        BattleManager._apply_status_to_enemy(i, "poison", value)
        RelicRes.EffectType.GAIN_MORALE:
            if is_inside_tree() and BattleManager:
                BattleManager._apply_status_to_hero(relic.owner_hero_id, "morale", value)
        RelicRes.EffectType.MAX_HP:
            if is_inside_tree():
                for hero in TeamManager.heroes:
                    TeamManager.increase_max_hp(hero.hero_id, value)
```

- [ ] **Step 3: complete_battle()에서 BATTLE_WIN 트리거 + 릴릭 보상**

```gdscript
func complete_battle(won: bool) -> void:
    if won:
        trigger_relics(RelicResource.TriggerType.BATTLE_WIN)
        card_rewards = _generate_card_rewards()
        # 보스 처치 시 릭 보상 추가
        var node: MapNodeResource = run_map[current_node_id]
        if node.room_type == MapNodeResource.RoomType.BOSS:
            var relic := get_random_relic()
            if relic: add_relic(relic)
        change_state(GameState.CARD_PICK)
        _request_scene("res://scenes/card_pick/card_pick_scene.tscn")
```

---

## Task 3: BattleManager — 릴릭 트리거 훅

- [ ] **Step 1: start_player_turn()에 PLAYER_TURN_START 트리거 추가**

```gdscript
func start_player_turn() -> void:
    # ... 기존 코드 ...
    if GameManager and GameManager.is_inside_tree():
        GameManager.trigger_relics(
            load("res://resources/relic_resource.gd").TriggerType.PLAYER_TURN_START)
    player_turn_started.emit()
```

- [ ] **Step 2: end_player_turn()에 PLAYER_TURN_END 트리거**

- [ ] **Step 3: _deal_damage_to_hero()에 ON_HERO_DAMAGED 트리거**

```gdscript
if amount > 0:
    team_mgr.take_damage(hero_id, amount)
    hero_damaged.emit(hero_id, amount)
    if GameManager and GameManager.is_inside_tree():
        GameManager.trigger_relics(
            RelicResource.TriggerType.ON_HERO_DAMAGED,
            {"hero_id": hero_id, "amount": amount})
```

- [ ] **Step 4: setup_battle()에 BATTLE_START 트리거**

```gdscript
is_battle_active = true
battle_started.emit()
if GameManager and GameManager.is_inside_tree():
    GameManager.trigger_relics(RelicResource.TriggerType.BATTLE_START)
```

> **주의:** BattleManager는 Autoload이므로 GameManager 직접 참조 가능 (`GameManager.trigger_relics(...)`).

---

## Task 4: TeamManager.increase_max_hp() 추가

```gdscript
func increase_max_hp(hero_id: String, amount: int) -> void:
    for hero in heroes:
        if hero.hero_id == hero_id:
            hero.max_hp += amount
            # 현재 HP도 같이 증가
            _current_hp[hero_id] = _current_hp.get(hero_id, hero.max_hp) + amount
```

---

## Task 5: UI — 릴릭 표시

- [ ] **Step 1: MapScene에 릴릭 HBoxContainer 추가**

상단 또는 하단에 획득한 릴릭 이름 레이블 표시.

```gdscript
func _refresh_relics() -> void:
    for child in relic_container.get_children():
        child.queue_free()
    for relic in GameManager.relics:
        var lbl := Label.new()
        lbl.text = "[%s]" % relic.relic_name
        relic_container.add_child(lbl)
```

- [ ] **Step 2: BattleScene에도 동일한 릴릭 표시 추가**

---

## Task 6: 테스트 + 커밋

- [ ] **Step 1: test_relics.gd 작성 (~8개)**

```gdscript
# tests/test_relics.gd
class_name TestRelics
extends RefCounted
# relic_pool_size == 10
# trigger BATTLE_START → 독약 병 효과 적용 확인
# 전용 캐릭터 사망 시 효과 없음
# get_random_relic 중복 없음
# 보스 보상에 릴릭 포함
```

- [ ] **Step 2: test_runner.gd에 TestRelics 추가**

- [ ] **Step 3: 전체 테스트 통과**

예상: `=== Results: ~105 passed, 0 failed ===`

- [ ] **Step 4: 커밋**

```bash
git commit -m "feat: Plan 08 — 릴릭 10종 + 트리거 시스템"
```
