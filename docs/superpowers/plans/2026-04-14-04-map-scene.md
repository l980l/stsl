# Plan 04: 맵 씬 + 씬 전환 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 맵 탐험 → 전투 → 카드 보상 → 맵 복귀 핵심 루프를 연결하여 플레이어블 프로토타입을 완성한다.

**Architecture:** MapNodeResource + MapGenerator(RefCounted)로 10층 분기 맵 데이터를 생성. GameManager가 런 스테이트(run_map, available_node_ids, pending_enemies, card_rewards)를 관리하고 씬 전환을 처리. MapScene은 맵 표시 + 노드 클릭, CardPickScene은 카드 보상 선택, BattleScene은 GameManager.pending_enemies를 사용해 배틀을 시작.

**Tech Stack:** Godot 4.6, GDScript, 헤드리스 테스트 (기존 65개 + 신규 ~8개 → 73개)

---

## 브랜치

```bash
git checkout -b feat/plan-04-map-scene
```

---

## 파일 구조

```
resources/
  map_node_resource.gd       ← (신규) 맵 노드 데이터 (룸 타입, 연결 정보)
autoload/
  map_generator.gd           ← (신규) 10층 맵 생성 유틸 (RefCounted, Autoload 아님)
  game_manager.gd            ← (수정) 런 스테이트 변수 + 메서드 추가
scenes/
  map/
    map_scene.tscn            ← (신규) 루트 Node2D + 스크립트
    map_scene.gd              ← (신규) 맵 UI 빌드 + 노드 클릭 처리
  card_pick/
    card_pick_scene.tscn      ← (신규) 루트 Node2D + 스크립트
    card_pick_scene.gd        ← (신규) 카드 보상 선택 UI
  battle/
    battle_scene.gd           ← (수정) _start_battle() 로 변경, GameManager 연결
tests/
  test_map_generator.gd      ← (신규) MapGenerator 테스트 4개
  test_game_manager.gd       ← (신규) GameManager 런 스테이트 테스트 4개
  test_runner.gd             ← (수정) 2개 스위트 추가
project.godot                ← (수정) main_scene → map_scene.tscn
```

---

## 맵 구조

```
Floor 9 (y=900-9×88=108): 👑 보스  (ID 27, col 1 고정)
Floor 8 (y=196):          💀💀💀  엘리트 전투 3개 (ID 24~26)
...
Floor 0 (y=900):          ⚔⚔⚔  일반 전투 3개 (ID 0~2)

node_id = floor_num × 3 + col   (floor 0~8)
node_id = 27                     (boss)

COL_X = [760, 960, 1160]
FLOOR_Y = 900 - floor_num × 88
```

룸 타입 배치:
| Floor | col 0 | col 1 | col 2 |
|-------|-------|-------|-------|
| 0~2   | BATTLE | BATTLE | BATTLE |
| 3     | BATTLE | REST  | SHOP  |
| 4     | BATTLE | ELITE | BATTLE |
| 5     | BATTLE | BATTLE | BATTLE |
| 6     | ELITE | BATTLE | SHOP  |
| 7     | BATTLE | SHOP  | REST  |
| 8     | ELITE | ELITE | ELITE |
| 9     | — | BOSS | — |

---

## Task 1: MapNodeResource + MapGenerator + 테스트

**Files:**
- Create: `resources/map_node_resource.gd`
- Create: `autoload/map_generator.gd`
- Create: `tests/test_map_generator.gd`
- Modify: `tests/test_runner.gd`

- [ ] **Step 1: map_node_resource.gd 생성**

```gdscript
# resources/map_node_resource.gd
class_name MapNodeResource
extends Resource

enum RoomType { BATTLE, ELITE, REST, SHOP, BOSS }

@export var node_id: int = 0
@export var floor_num: int = 0      # 0 = 첫째 층, 9 = 보스 층
@export var column: int = 0         # 0, 1, 2
@export var room_type: RoomType = RoomType.BATTLE
@export var connections: Array = [] # 다음 층 연결 node_id 목록
@export var visited: bool = false
```

- [ ] **Step 2: map_generator.gd 스텁 생성 (TDD — 테스트 먼저 실패시키기 위한 빈 반환)**

```gdscript
# autoload/map_generator.gd
# NOTE: Autoload에 등록하지 않음 — RefCounted 유틸 클래스
class_name MapGenerator
extends RefCounted

const FLOORS := 10
const COLS := 3

static func generate() -> Array:
	return []  # 스텁 — Step 5에서 구현
```

- [ ] **Step 3: test_map_generator.gd 작성**

```gdscript
# tests/test_map_generator.gd
class_name TestMapGenerator
extends RefCounted

const MapGen = preload("res://autoload/map_generator.gd")
const MapNodeRes = preload("res://resources/map_node_resource.gd")

var passed: int = 0
var failed: int = 0

func run_all() -> Dictionary:
	test_node_count()
	test_boss_node()
	test_connections_valid()
	test_floor0_nodes()
	return {"passed": passed, "failed": failed}

func _assert(cond: bool, msg: String) -> void:
	if cond:
		passed += 1
		print("  PASS: " + msg)
	else:
		failed += 1
		push_error("  FAIL: " + msg)

func test_node_count() -> void:
	print("[TestMapGenerator] test_node_count")
	var map := MapGen.generate()
	_assert(map.size() == 28, "노드 수 == 28 (floor 0~8 × 3 + 보스 1)")

func test_boss_node() -> void:
	print("[TestMapGenerator] test_boss_node")
	var map := MapGen.generate()
	_assert(map.size() == 28, "보스 테스트를 위해 노드 수 확인")
	if map.size() < 28:
		return
	var boss = map[27]
	_assert(boss.room_type == MapNodeRes.RoomType.BOSS, "ID 27 = BOSS 타입")
	_assert(boss.floor_num == 9, "보스 층 == 9")
	_assert(boss.connections.is_empty(), "보스는 연결 없음")

func test_connections_valid() -> void:
	print("[TestMapGenerator] test_connections_valid")
	var map := MapGen.generate()
	if map.size() < 28:
		_assert(false, "노드 부족으로 건너뜀")
		return
	for node in map:
		for conn_id in node.connections:
			_assert(conn_id >= 0 and conn_id < map.size(),
				"연결 ID %d 범위 유효" % conn_id)
			_assert(map[conn_id].floor_num == node.floor_num + 1,
				"연결 노드는 다음 층")

func test_floor0_nodes() -> void:
	print("[TestMapGenerator] test_floor0_nodes")
	var map := MapGen.generate()
	if map.size() < 3:
		_assert(false, "노드 부족")
		return
	_assert(map[0].floor_num == 0 and map[0].column == 0, "ID 0: floor 0, col 0")
	_assert(map[1].floor_num == 0 and map[1].column == 1, "ID 1: floor 0, col 1")
	_assert(map[2].floor_num == 0 and map[2].column == 2, "ID 2: floor 0, col 2")
	_assert(not map[0].connections.is_empty(), "floor 0 노드에 연결 있음")
```

- [ ] **Step 4: test_runner.gd에 TestMapGenerator 추가**

```gdscript
# tests/test_runner.gd
extends SceneTree

var TestResources = preload("res://tests/test_resources.gd")
var TestTeamManager = preload("res://tests/test_team_manager.gd")
var TestDeckManager = preload("res://tests/test_deck_manager.gd")
var TestBattleManager = preload("res://tests/test_battle_manager.gd")
var TestMapGenerator = preload("res://tests/test_map_generator.gd")

func _init() -> void:
	var total_passed: int = 0
	var total_failed: int = 0
	var suites: Array = [
		TestResources.new(),
		TestTeamManager.new(),
		TestDeckManager.new(),
		TestBattleManager.new(),
		TestMapGenerator.new(),
	]
	for suite in suites:
		var result: Dictionary = suite.run_all()
		total_passed += result.passed
		total_failed += result.failed
	print("\n=== Results: %d passed, %d failed ===" % [total_passed, total_failed])
	quit(1 if total_failed > 0 else 0)
```

- [ ] **Step 5: 테스트 실행 (실패 확인)**

```bash
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl/tests/test_runner.gd" --path "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
```

예상: `TestMapGenerator` 테스트 실패 (`노드 수 == 28` 등 실패)

- [ ] **Step 6: map_generator.gd 구현**

스텁의 `generate()` 를 아래로 교체:

```gdscript
# autoload/map_generator.gd
class_name MapGenerator
extends RefCounted

const FLOORS := 10
const COLS := 3

static func generate() -> Array:
	var nodes: Array = []

	# Floor 0~8: 각 3개 노드 (총 27개)
	for f in range(FLOORS - 1):
		for c in range(COLS):
			var node := MapNodeResource.new()
			node.node_id = f * COLS + c
			node.floor_num = f
			node.column = c
			node.room_type = _pick_room_type(f, c)
			nodes.append(node)

	# Floor 9: 보스 노드 (ID 27)
	var boss := MapNodeResource.new()
	boss.node_id = 27
	boss.floor_num = 9
	boss.column = 1
	boss.room_type = MapNodeResource.RoomType.BOSS
	boss.connections = []
	nodes.append(boss)

	# 연결 생성: floor 0~7 → 다음 floor
	for f in range(FLOORS - 2):  # 0~7
		for c in range(COLS):
			nodes[f * COLS + c].connections = _make_connections(f, c)

	# Floor 8 → 보스 (ID 27)
	for c in range(COLS):
		nodes[8 * COLS + c].connections = [27]

	return nodes

static func _pick_room_type(floor_num: int, col: int) -> MapNodeResource.RoomType:
	if floor_num == 3 and col == 1: return MapNodeResource.RoomType.REST
	if floor_num == 3 and col == 2: return MapNodeResource.RoomType.SHOP
	if floor_num == 4 and col == 1: return MapNodeResource.RoomType.ELITE
	if floor_num == 6 and col == 0: return MapNodeResource.RoomType.ELITE
	if floor_num == 6 and col == 2: return MapNodeResource.RoomType.SHOP
	if floor_num == 7 and col == 1: return MapNodeResource.RoomType.SHOP
	if floor_num == 7 and col == 2: return MapNodeResource.RoomType.REST
	if floor_num == 8:               return MapNodeResource.RoomType.ELITE
	return MapNodeResource.RoomType.BATTLE

# col 0 → 다음 floor col 0~1
# col 1 → 다음 floor col 0~2
# col 2 → 다음 floor col 1~2
static func _make_connections(floor_num: int, col: int) -> Array:
	var base := (floor_num + 1) * COLS
	match col:
		0: return [base + 0, base + 1]
		1: return [base + 0, base + 1, base + 2]
		2: return [base + 1, base + 2]
		_: return [base + 1]
```

- [ ] **Step 7: 테스트 실행 (통과 확인)**

```bash
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl/tests/test_runner.gd" --path "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
```

예상: `=== Results: 69 passed, 0 failed ===`

- [ ] **Step 8: 커밋**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
git add resources/map_node_resource.gd autoload/map_generator.gd tests/test_map_generator.gd tests/test_runner.gd
git commit -m "feat: MapNodeResource + MapGenerator + 테스트 (Plan 04 Task 1)"
```

---

## Task 2: GameManager 런 스테이트 확장 + 테스트

**Files:**
- Modify: `autoload/game_manager.gd`
- Create: `tests/test_game_manager.gd`
- Modify: `tests/test_runner.gd`

- [ ] **Step 1: test_game_manager.gd 작성 (TDD — 구현 전 실패 확인용)**

```gdscript
# tests/test_game_manager.gd
class_name TestGameManager
extends RefCounted

const GameManagerClass = preload("res://autoload/game_manager.gd")
const MapGen = preload("res://autoload/map_generator.gd")

var passed: int = 0
var failed: int = 0

func run_all() -> Dictionary:
	test_run_map_initialized()
	test_enter_node_marks_visited()
	test_enter_battle_sets_pending_enemies()
	test_complete_battle_generates_rewards()
	return {"passed": passed, "failed": failed}

func _assert(cond: bool, msg: String) -> void:
	if cond:
		passed += 1
		print("  PASS: " + msg)
	else:
		failed += 1
		push_error("  FAIL: " + msg)

func _make_gm() -> GameManagerClass:
	var gm := GameManagerClass.new()
	gm.run_map = MapGen.generate()
	gm.available_node_ids = [0, 1, 2]
	gm.current_node_id = -1
	gm.pending_enemies = []
	gm.card_rewards = []
	return gm

func test_run_map_initialized() -> void:
	print("[TestGameManager] test_run_map_initialized")
	var gm := _make_gm()
	_assert(gm.run_map.size() == 28, "맵 28개 노드")
	_assert(gm.available_node_ids == [0, 1, 2], "초기 접근 가능 노드 [0,1,2]")

func test_enter_node_marks_visited() -> void:
	print("[TestGameManager] test_enter_node_marks_visited")
	var gm := _make_gm()
	_assert(not gm.run_map[0].visited, "초기 미방문")
	gm.enter_node(0)
	_assert(gm.run_map[0].visited, "enter_node 후 visited = true")
	_assert(gm.current_node_id == 0, "current_node_id == 0")

func test_enter_battle_sets_pending_enemies() -> void:
	print("[TestGameManager] test_enter_battle_sets_pending_enemies")
	var gm := _make_gm()
	# node 0 = floor 0, col 0 = BATTLE 타입
	gm.enter_node(0)
	_assert(gm.pending_enemies.size() > 0, "BATTLE 노드 진입 시 pending_enemies 설정")

func test_complete_battle_generates_rewards() -> void:
	print("[TestGameManager] test_complete_battle_generates_rewards")
	var gm := _make_gm()
	gm.enter_node(0)
	gm.complete_battle(true)
	_assert(gm.card_rewards.size() == 3, "전투 승리 시 카드 보상 3장")
```

- [ ] **Step 2: test_runner.gd에 TestGameManager 추가**

```gdscript
# tests/test_runner.gd
extends SceneTree

var TestResources = preload("res://tests/test_resources.gd")
var TestTeamManager = preload("res://tests/test_team_manager.gd")
var TestDeckManager = preload("res://tests/test_deck_manager.gd")
var TestBattleManager = preload("res://tests/test_battle_manager.gd")
var TestMapGenerator = preload("res://tests/test_map_generator.gd")
var TestGameManager = preload("res://tests/test_game_manager.gd")

func _init() -> void:
	var total_passed: int = 0
	var total_failed: int = 0
	var suites: Array = [
		TestResources.new(),
		TestTeamManager.new(),
		TestDeckManager.new(),
		TestBattleManager.new(),
		TestMapGenerator.new(),
		TestGameManager.new(),
	]
	for suite in suites:
		var result: Dictionary = suite.run_all()
		total_passed += result.passed
		total_failed += result.failed
	print("\n=== Results: %d passed, %d failed ===" % [total_passed, total_failed])
	quit(1 if total_failed > 0 else 0)
```

- [ ] **Step 3: 테스트 실행 (실패 확인 — pending_enemies, card_rewards 등 미선언)**

```bash
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl/tests/test_runner.gd" --path "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
```

예상: TestGameManager 테스트 실패 또는 에러

- [ ] **Step 4: game_manager.gd 확장**

기존 파일에 아래를 추가한다. `var relics: Array = []` 아래에 새 변수 추가, `reset()` 수정, 새 메서드 추가:

```gdscript
# autoload/game_manager.gd
class_name GameManagerClass
extends Node

enum GameState { MAP, BATTLE, CARD_PICK, EVENT, SHOP, REST }

var current_state: GameState = GameState.MAP
var current_floor: int = 0
var current_chapter: int = 1
var gold: int = 0
var relics: Array = []

# ── Plan 04: 런 스테이트 ──────────────────────────────
var run_map: Array = []             # Array[MapNodeResource]
var available_node_ids: Array = []  # 현재 클릭 가능한 노드 ID 목록
var current_node_id: int = -1
var pending_enemies: Array = []     # 다음 배틀 적 데이터
var card_rewards: Array = []        # 다음 카드 보상 목록
# ─────────────────────────────────────────────────────

signal state_changed(new_state: GameState)
signal gold_changed(new_gold: int)
signal relic_added(relic: Resource)
signal run_started()
signal node_entered(node_id: int)
signal run_ended(won: bool)

func change_state(new_state: GameState) -> void:
	current_state = new_state
	state_changed.emit(new_state)

func add_gold(amount: int) -> void:
	gold += amount
	gold_changed.emit(gold)

func spend_gold(amount: int) -> bool:
	if gold < amount:
		return false
	gold -= amount
	gold_changed.emit(gold)
	return true

func add_relic(relic: Resource) -> void:
	relics.append(relic)
	relic_added.emit(relic)

func has_relic(relic_name: String) -> bool:
	for r in relics:
		if r.relic_name == relic_name:
			return true
	return false

func reset() -> void:
	current_state = GameState.MAP
	current_floor = 0
	current_chapter = 1
	gold = 0
	relics.clear()
	run_map.clear()
	available_node_ids.clear()
	current_node_id = -1
	pending_enemies.clear()
	card_rewards.clear()

# ── Plan 04: 런 관리 ──────────────────────────────────

func start_run() -> void:
	reset()

	# TeamManager 초기화 (나폴레옹 1명)
	TeamManager.clear()
	var HeroRes = load("res://resources/hero_resource.gd")
	var napoleon := HeroRes.new()
	napoleon.hero_id = "napoleon"
	napoleon.hero_name = "나폴레옹"
	napoleon.max_hp = 70
	napoleon.character_scene = load("res://characters/heroes/napoleon/napoleon.tscn")
	TeamManager.add_hero(napoleon)

	# DeckManager 초기화 (스트라이크 3 + 디펜드 2)
	DeckManager.clear()
	var CardRes = load("res://resources/card_resource.gd")
	var EffRes = load("res://resources/effect_resource.gd")
	for _i in range(3):
		var card := CardRes.new()
		card.card_name = "스트라이크"
		card.owner_id = "napoleon"
		card.cost = 1
		card.play_animation = "attack"
		var eff := EffRes.new()
		eff.effect_type = EffRes.EffectType.DAMAGE
		eff.value = 6
		eff.target = "SINGLE"
		card.effects = [eff]
		DeckManager.add_card_to_deck(card)
	for _i in range(2):
		var card := CardRes.new()
		card.card_name = "디펜드"
		card.owner_id = "napoleon"
		card.cost = 1
		card.play_animation = "idle"
		var eff := EffRes.new()
		eff.effect_type = EffRes.EffectType.BLOCK
		eff.value = 5
		card.effects = [eff]
		DeckManager.add_card_to_deck(card)

	# 맵 생성
	run_map = MapGenerator.generate()
	available_node_ids = [0, 1, 2]  # floor 0 전체 접근 가능
	run_started.emit()
	# 씬 전환 없음 — MapScene._ready()에서 호출되므로 이미 MapScene에 있음

func enter_node(node_id: int) -> void:
	if node_id not in available_node_ids:
		return
	var node: MapNodeResource = run_map[node_id]
	node.visited = true
	current_node_id = node_id
	current_floor = node.floor_num
	node_entered.emit(node_id)

	match node.room_type:
		MapNodeResource.RoomType.BATTLE, \
		MapNodeResource.RoomType.ELITE, \
		MapNodeResource.RoomType.BOSS:
			pending_enemies = _make_enemies_for_node(node)
			change_state(GameState.BATTLE)
			_request_scene("res://scenes/battle/battle_scene.tscn")
		MapNodeResource.RoomType.REST:
			_heal_all_heroes(10)
			_advance_nodes_from(node_id)
			change_state(GameState.MAP)
			_request_scene("res://scenes/map/map_scene.tscn")
		MapNodeResource.RoomType.SHOP:
			# Plan 05에서 구현 — 지금은 건너뜀
			_advance_nodes_from(node_id)
			change_state(GameState.MAP)
			_request_scene("res://scenes/map/map_scene.tscn")

func complete_battle(won: bool) -> void:
	if won:
		card_rewards = _generate_card_rewards()
		change_state(GameState.CARD_PICK)
		_request_scene("res://scenes/card_pick/card_pick_scene.tscn")
	else:
		run_ended.emit(false)
		change_state(GameState.MAP)
		_request_scene("res://scenes/map/map_scene.tscn")

func complete_card_pick() -> void:
	var node: MapNodeResource = run_map[current_node_id]
	_advance_nodes_from(current_node_id)
	if node.room_type == MapNodeResource.RoomType.BOSS:
		run_ended.emit(true)
	card_rewards.clear()
	change_state(GameState.MAP)
	_request_scene("res://scenes/map/map_scene.tscn")

func _advance_nodes_from(node_id: int) -> void:
	var node: MapNodeResource = run_map[node_id]
	available_node_ids = node.connections.duplicate()

func _heal_all_heroes(amount: int) -> void:
	if not is_inside_tree():
		return
	for hero in TeamManager.heroes:
		TeamManager.heal(hero.hero_id, amount)

func _make_enemies_for_node(node: MapNodeResource) -> Array:
	var satyr_scene = load("res://characters/enemies/satyr/satyr.tscn")
	match node.room_type:
		MapNodeResource.RoomType.BATTLE:
			return [_make_satyr(satyr_scene, 30, 6)]
		MapNodeResource.RoomType.ELITE:
			return [_make_satyr(satyr_scene, 45, 8),
					_make_satyr(satyr_scene, 30, 6)]
		MapNodeResource.RoomType.BOSS:
			return [_make_satyr(satyr_scene, 80, 10)]
		_:
			return []

func _make_satyr(scene: PackedScene, hp: int, dmg: int) -> Resource:
	var EnemyRes = load("res://resources/enemy_resource.gd")
	var IntentRes = load("res://resources/intent_resource.gd")
	var enemy := EnemyRes.new()
	enemy.enemy_name = "사티로스"
	enemy.max_hp = hp
	enemy.character_scene = scene
	var intent := IntentRes.new()
	intent.action_type = IntentRes.ActionType.ATTACK
	intent.value = dmg
	intent.target = IntentRes.TargetType.RANDOM
	enemy.intent_pattern = [intent]
	return enemy

func _generate_card_rewards() -> Array:
	var pool := _napoleon_card_pool()
	pool.shuffle()
	return pool.slice(0, 3)

func _napoleon_card_pool() -> Array:
	var CardRes = load("res://resources/card_resource.gd")
	var EffRes = load("res://resources/effect_resource.gd")
	var cards := []

	# 1. 헤비 스트라이크 — DAMAGE 9, cost 2
	var c1 := CardRes.new()
	c1.card_name = "헤비 스트라이크"
	c1.owner_id = "napoleon"
	c1.cost = 2
	c1.play_animation = "attack"
	var e1 := EffRes.new()
	e1.effect_type = EffRes.EffectType.DAMAGE
	e1.value = 9
	e1.target = "SINGLE"
	c1.effects = [e1]
	cards.append(c1)

	# 2. 클리브 — ALL DAMAGE 4, cost 1
	var c2 := CardRes.new()
	c2.card_name = "클리브"
	c2.owner_id = "napoleon"
	c2.cost = 1
	c2.play_animation = "attack"
	var e2 := EffRes.new()
	e2.effect_type = EffRes.EffectType.DAMAGE
	e2.value = 4
	e2.target = "ALL"
	c2.effects = [e2]
	cards.append(c2)

	# 3. 아이언 웨이브 — DAMAGE 5 + BLOCK 5, cost 1
	var c3 := CardRes.new()
	c3.card_name = "아이언 웨이브"
	c3.owner_id = "napoleon"
	c3.cost = 1
	c3.play_animation = "attack"
	var e3a := EffRes.new()
	e3a.effect_type = EffRes.EffectType.DAMAGE
	e3a.value = 5
	e3a.target = "SINGLE"
	var e3b := EffRes.new()
	e3b.effect_type = EffRes.EffectType.BLOCK
	e3b.value = 5
	c3.effects = [e3a, e3b]
	cards.append(c3)

	# 4. 아이언 디펜스 — BLOCK 8, cost 1
	var c4 := CardRes.new()
	c4.card_name = "아이언 디펜스"
	c4.owner_id = "napoleon"
	c4.cost = 1
	c4.play_animation = "idle"
	var e4 := EffRes.new()
	e4.effect_type = EffRes.EffectType.BLOCK
	e4.value = 8
	c4.effects = [e4]
	cards.append(c4)

	# 5. 포이즌 스트라이크 — DAMAGE 3 + poison 2, cost 1
	var c5 := CardRes.new()
	c5.card_name = "포이즌 스트라이크"
	c5.owner_id = "napoleon"
	c5.cost = 1
	c5.play_animation = "attack"
	var e5a := EffRes.new()
	e5a.effect_type = EffRes.EffectType.DAMAGE
	e5a.value = 3
	e5a.target = "SINGLE"
	var e5b := EffRes.new()
	e5b.effect_type = EffRes.EffectType.APPLY_STATUS
	e5b.status_type = "poison"
	e5b.value = 2
	e5b.target = "SINGLE"
	c5.effects = [e5a, e5b]
	cards.append(c5)

	return cards

func _request_scene(path: String) -> void:
	var tree := get_tree()
	if tree != null:
		tree.change_scene_to_file(path)
```

- [ ] **Step 5: 테스트 실행 (통과 확인)**

```bash
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl/tests/test_runner.gd" --path "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
```

예상: `=== Results: 73 passed, 0 failed ===`

- [ ] **Step 6: 커밋**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
git add autoload/game_manager.gd tests/test_game_manager.gd tests/test_runner.gd
git commit -m "feat: GameManager 런 스테이트 + 씬 전환 + 테스트 (Plan 04 Task 2)"
```

---

## Task 3: MapScene UI

**Files:**
- Create: `scenes/map/map_scene.tscn`
- Create: `scenes/map/map_scene.gd`

- [ ] **Step 1: scenes/map/ 디렉토리 생성 + map_scene.tscn 생성**

```
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scenes/map/map_scene.gd" id="1"]

[node name="MapScene" type="Node2D"]
script = ExtResource("1")
```

- [ ] **Step 2: map_scene.gd 생성**

```gdscript
# scenes/map/map_scene.gd
extends Node2D

const MapNodeRes = preload("res://resources/map_node_resource.gd")

const COL_X := [760, 960, 1160]
const FLOOR_Y_BOTTOM := 900
const FLOOR_GAP := 88
const NODE_W := 120
const NODE_H := 50

var _node_buttons: Dictionary = {}  # node_id → Button
var _floor_label: Label

func _ready() -> void:
	if GameManager.run_map.is_empty():
		GameManager.start_run()
	_build_ui()
	_refresh_map()

func _build_ui() -> void:
	# 배경
	var bg := ColorRect.new()
	bg.color = Color(0.05, 0.05, 0.1)
	bg.position = Vector2.ZERO
	bg.size = Vector2(1920, 1080)
	add_child(bg)

	# 제목
	var title := Label.new()
	title.text = "탐험 맵"
	title.position = Vector2(860, 20)
	title.size = Vector2(200, 50)
	title.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	title.add_theme_font_size_override("font_size", 30)
	add_child(title)

	# 층 정보
	_floor_label = Label.new()
	_floor_label.position = Vector2(50, 520)
	_floor_label.size = Vector2(300, 40)
	_floor_label.add_theme_font_size_override("font_size", 18)
	add_child(_floor_label)

	# 연결선 먼저 그리기 (버튼 뒤에)
	_draw_connections()

	# 노드 버튼 생성
	for node in GameManager.run_map:
		_create_node_button(node)

func _draw_connections() -> void:
	for node in GameManager.run_map:
		var from := _node_center(node)
		for conn_id in node.connections:
			var to := _node_center(GameManager.run_map[conn_id])
			var line := Line2D.new()
			line.add_point(from)
			line.add_point(to)
			line.width = 3.0
			line.default_color = Color(0.4, 0.4, 0.5, 0.6)
			add_child(line)

func _create_node_button(node: Resource) -> void:
	var btn := Button.new()
	btn.position = _node_top_left(node)
	btn.size = Vector2(NODE_W, NODE_H)
	btn.text = _room_type_text(node.room_type)
	btn.add_theme_font_size_override("font_size", 13)
	var captured_id: int = node.node_id
	btn.pressed.connect(func(): GameManager.enter_node(captured_id))
	add_child(btn)
	_node_buttons[node.node_id] = btn

func _refresh_map() -> void:
	for node_id in _node_buttons:
		var btn: Button = _node_buttons[node_id]
		var node: Resource = GameManager.run_map[node_id]

		if node.visited:
			btn.disabled = true
			btn.modulate = Color(0.5, 0.5, 0.5)
		elif node_id in GameManager.available_node_ids:
			btn.disabled = false
			btn.modulate = Color(1.0, 1.0, 1.0)
		else:
			btn.disabled = true
			btn.modulate = Color(0.6, 0.6, 0.7, 0.5)

		if node_id == GameManager.current_node_id:
			btn.modulate = Color(1.0, 1.0, 0.3)

	_floor_label.text = "현재 층: %d / 9" % GameManager.current_floor

func _node_center(node: Resource) -> Vector2:
	return Vector2(
		COL_X[node.column],
		FLOOR_Y_BOTTOM - node.floor_num * FLOOR_GAP
	)

func _node_top_left(node: Resource) -> Vector2:
	return _node_center(node) - Vector2(NODE_W / 2.0, NODE_H / 2.0)

func _room_type_text(room_type: int) -> String:
	match room_type:
		MapNodeRes.RoomType.BATTLE: return "⚔ 전투"
		MapNodeRes.RoomType.ELITE: return "💀 엘리트"
		MapNodeRes.RoomType.REST: return "🔥 휴식"
		MapNodeRes.RoomType.SHOP: return "🏪 상점"
		MapNodeRes.RoomType.BOSS: return "👑 보스"
		_: return "?"
```

- [ ] **Step 3: 기존 테스트 회귀 확인**

```bash
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl/tests/test_runner.gd" --path "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
```

예상: `=== Results: 73 passed, 0 failed ===`

- [ ] **Step 4: 커밋**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
git add scenes/map/map_scene.tscn scenes/map/map_scene.gd
git commit -m "feat: MapScene UI — 맵 표시 + 노드 선택 (Plan 04 Task 3)"
```

---

## Task 4: CardPickScene

**Files:**
- Create: `scenes/card_pick/card_pick_scene.tscn`
- Create: `scenes/card_pick/card_pick_scene.gd`

- [ ] **Step 1: scenes/card_pick/ 디렉토리 생성 + card_pick_scene.tscn 생성**

```
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scenes/card_pick/card_pick_scene.gd" id="1"]

[node name="CardPickScene" type="Node2D"]
script = ExtResource("1")
```

- [ ] **Step 2: card_pick_scene.gd 생성**

```gdscript
# scenes/card_pick/card_pick_scene.gd
extends Node2D

const CARD_W := 140
const CARD_H := 200

func _ready() -> void:
	_build_ui()

func _build_ui() -> void:
	# 배경
	var bg := ColorRect.new()
	bg.color = Color(0.05, 0.05, 0.1)
	bg.position = Vector2.ZERO
	bg.size = Vector2(1920, 1080)
	add_child(bg)

	# 제목
	var title := Label.new()
	title.text = "카드를 선택하세요 (1장)"
	title.position = Vector2(660, 100)
	title.size = Vector2(600, 60)
	title.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	title.add_theme_font_size_override("font_size", 32)
	add_child(title)

	var rewards := GameManager.card_rewards
	if rewards.is_empty():
		# 보상 없음 (e.g. 휴식 룸) — 바로 맵으로
		GameManager.complete_card_pick()
		return

	var total_w: float = rewards.size() * (CARD_W + 20) - 20
	var start_x: float = (1920.0 - total_w) / 2.0

	for i in range(rewards.size()):
		var card: Resource = rewards[i]
		var btn := Button.new()
		btn.position = Vector2(start_x + i * (CARD_W + 20), 400)
		btn.size = Vector2(CARD_W, CARD_H)
		var card_name: String = card.get("card_name") if card.get("card_name") != null else "?"
		btn.text = "[%d]\n%s\n%s" % [card.cost, card_name, card.get("owner_id") if card.get("owner_id") != null else ""]
		btn.add_theme_font_size_override("font_size", 15)
		var captured_card := card
		btn.pressed.connect(func(): _on_card_selected(captured_card))
		add_child(btn)

	# 건너뛰기 버튼
	var skip_btn := Button.new()
	skip_btn.position = Vector2(880, 680)
	skip_btn.size = Vector2(160, 50)
	skip_btn.text = "건너뛰기"
	skip_btn.add_theme_font_size_override("font_size", 18)
	skip_btn.pressed.connect(_on_skip)
	add_child(skip_btn)

func _on_card_selected(card: Resource) -> void:
	DeckManager.add_card_to_deck(card)
	GameManager.complete_card_pick()

func _on_skip() -> void:
	GameManager.complete_card_pick()
```

- [ ] **Step 3: 기존 테스트 회귀 확인**

```bash
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl/tests/test_runner.gd" --path "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
```

예상: `=== Results: 73 passed, 0 failed ===`

- [ ] **Step 4: 커밋**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
git add scenes/card_pick/card_pick_scene.tscn scenes/card_pick/card_pick_scene.gd
git commit -m "feat: CardPickScene 카드 보상 선택 (Plan 04 Task 4)"
```

---

## Task 5: BattleScene 연결 + project.godot + 최종 확인

**Files:**
- Modify: `scenes/battle/battle_scene.gd`
- Modify: `project.godot`

- [ ] **Step 1: battle_scene.gd — _ready() + _start_battle() 수정**

`func _ready()` 안의 `_start_test_battle()` 호출을 `_start_battle()`로 교체:

```gdscript
func _ready() -> void:
	_build_ui()
	BattleManager.team_mgr = TeamManager
	BattleManager.deck_mgr = DeckManager
	_connect_signals()
	_start_battle()
```

기존 `func _start_test_battle()` 위에 새 함수 추가:

```gdscript
func _start_battle() -> void:
	if not GameManager.pending_enemies.is_empty():
		BattleManager.setup_battle(GameManager.pending_enemies)
		_setup_heroes()
		_setup_enemies()
		BattleManager.start_player_turn()
	else:
		_start_test_battle()  # GameManager 없이 단독 실행 시 폴백
```

- [ ] **Step 2: _on_battle_won() + _on_battle_lost() 수정**

기존:
```gdscript
func _on_battle_won() -> void:
	_message_label.text = "🏆 승리!"
	_end_turn_btn.disabled = true
	_selected_card = null
	for entry in _enemy_nodes:
		entry["btn"].disabled = true

func _on_battle_lost() -> void:
	_message_label.text = "💀 패배..."
	_end_turn_btn.disabled = true
	_selected_card = null
```

교체:
```gdscript
func _on_battle_won() -> void:
	_message_label.text = "🏆 승리!"
	_end_turn_btn.disabled = true
	_selected_card = null
	for entry in _enemy_nodes:
		entry["btn"].disabled = true
	GameManager.complete_battle(true)

func _on_battle_lost() -> void:
	_message_label.text = "💀 패배..."
	_end_turn_btn.disabled = true
	_selected_card = null
	GameManager.complete_battle(false)
```

- [ ] **Step 3: project.godot — main_scene 변경**

```ini
[application]

config/name="STSL"
config/features=PackedStringArray("4.6", "Mobile")
config/icon="res://icon.svg"
config/run/main_scene="res://scenes/map/map_scene.tscn"
```

- [ ] **Step 4: 최종 테스트 회귀 확인**

```bash
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl/tests/test_runner.gd" --path "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
```

예상: `=== Results: 73 passed, 0 failed ===`

- [ ] **Step 5: 시각 확인 (선택)**

```bash
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --path "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl" res://scenes/map/map_scene.tscn
```

확인 항목:
- 맵 창이 열리고 10층 노드 배치가 보임
- floor 0의 3개 노드(⚔ 전투)만 클릭 가능 (흰색)
- 나머지 노드는 반투명 비활성
- 노드 클릭 → BattleScene으로 전환
- 전투 승리 → CardPickScene으로 전환 (카드 3장 표시)
- 카드 선택 → MapScene으로 복귀 (방문한 노드 회색, 다음 층 활성화)

- [ ] **Step 6: 커밋**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
git add scenes/battle/battle_scene.gd project.godot
git commit -m "feat: BattleScene-GameManager 연결 + 메인 씬 맵으로 변경 (Plan 04 Task 5)"
```

---

## Self-Review

### Spec 커버리지

| 기능 | 태스크 |
|---|---|
| 10층 분기 맵 생성 | Task 1 (MapGenerator) |
| GameManager 런 스테이트 관리 | Task 2 |
| 맵 표시 + 노드 클릭 | Task 3 (MapScene) |
| 전투 후 카드 보상 선택 | Task 4 (CardPickScene) |
| BattleScene → GameManager 연결 | Task 5 |
| 씬 전환 (맵→배틀→카드픽→맵) | Task 2 (_request_scene) + Task 5 |
| 휴식 룸 즉시 회복 후 맵 복귀 | Task 2 (enter_node REST 분기) |
| 상점 플레이스홀더 | Task 2 (enter_node SHOP 분기 — 건너뜀) |
| 기존 65개 테스트 회귀 | 각 Task Step에서 확인 |

### 플레이스홀더 검사

없음. 상점(SHOP)은 Plan 05 범위로 명시, 현재는 노드 스킵 처리.

### 타입 일관성

- `MapNodeResource.RoomType` — Task 1에서 정의, Task 2~3에서 동일하게 참조
- `GameManager.pending_enemies` — Task 2에서 `var pending_enemies: Array` 선언, Task 5에서 `GameManager.pending_enemies.is_empty()` 사용
- `GameManager.complete_battle(true/false)` — Task 2에서 정의, Task 5에서 동일 시그니처 호출
- `MapGenerator.generate()` — Task 1에서 `static func generate() -> Array`, Task 2 `game_manager.gd`에서 `MapGenerator.generate()` 호출
