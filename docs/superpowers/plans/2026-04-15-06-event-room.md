# Plan 06: 이벤트 룸 시스템

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 맵의 이벤트 룸(EVENT 타입)을 구현한다. EventScene UI + 5종 이벤트 로직. 전투 보상을 제외한 비전투 상호작용 루프를 완성한다.

**Architecture:**
- `MapNodeResource.RoomType`에 `EVENT` 추가 (현재 BATTLE/ELITE/REST/SHOP/BOSS)
- `EventResource`: 이벤트 텍스트 + 선택지 목록
- `EventScene`: 이벤트 카드 텍스트 + 선택지 버튼, 결과 처리 후 MapScene으로 복귀
- `GameManager`: EVENT 상태 처리, `enter_node`에서 EVENT 룸 분기
- REST 룸: 기존 조용한 HP 회복 → RestScene으로 교체 (HP 회복 or 카드 강화 선택)

**Tech Stack:** Godot 4.6, GDScript, 헤드리스 테스트 (기존 ~81개 + 신규 ~6개 → ~87개)

---

## 브랜치

```bash
git checkout -b feat/plan-06-event-room
```

---

## 이벤트 5종 정의

| 이름 | 선택지 | 효과 |
|------|--------|------|
| **황금 상자** | A: 열기 | 골드 +30 |
| **황금 상자** | B: 무시 | 아무것도 없음 |
| **상처 입은 전사** | A: 치료 (골드 -20) | HP +15 회복 |
| **상처 입은 전사** | B: 무시 | 아무것도 없음 |
| **고대 도서관** | A: 공부 (HP -5) | 카드 드로우 +1 (DeckManager.base_draw_count) |
| **고대 도서관** | B: 무시 | 아무것도 없음 |
| **저주받은 제단** | A: 바치기 (카드 제거 1장) | 릴릭 1개 획득 (랜덤) |
| **저주받은 제단** | B: 무시 | 아무것도 없음 |
| **동료 만남** | A: 합류시키기 | 팀원 영입 (나폴레옹만 있다면 클레오파트라/이순신 중 랜덤) |
| **동료 만남** | B: 거절 | 아무것도 없음 |

> **주의:** 동료 영입은 Plan 07(클레오파트라/이순신 구현) 이후 완전히 동작. Plan 06에서는 골드/HP/드로우/카드제거까지 구현, 영입은 스텁(안내 메시지만)으로 남긴다.

---

## 파일 구조

```
resources/
  event_resource.gd         ← (신규) 이벤트 데이터
autoload/
  game_manager.gd            ← (수정) EVENT 상태 + enter_node EVENT 분기
  map_generator.gd           ← (수정) Floor 5, 6에 EVENT 룸 배치
  map_node_resource.gd       ← (수정) RoomType에 EVENT 추가
scenes/
  event/
    event_scene.tscn          ← (신규)
    event_scene.gd            ← (신규)
  rest/
    rest_scene.tscn           ← (신규) HP 회복 또는 카드 강화 선택
    rest_scene.gd             ← (신규)
tests/
  test_event.gd              ← (신규) 이벤트 로직 테스트 ~6개
  test_runner.gd             ← (수정)
```

---

## Task 1: MapNodeResource + MapGenerator에 EVENT 룸 추가

- [ ] **Step 1: map_node_resource.gd — RoomType에 EVENT 추가**

```gdscript
enum RoomType { BATTLE, ELITE, REST, SHOP, BOSS, EVENT }
```

- [ ] **Step 2: map_generator.gd — Floor 5, 6에 EVENT 배치**

`_pick_room_type`에 추가:
```gdscript
if floor_num == 5 and col == 1: return MapNodeRes.RoomType.EVENT
if floor_num == 6 and col == 1: return MapNodeRes.RoomType.EVENT  # 기존 BATTLE → EVENT
```

> Floor 6, col 0은 ELITE, col 2는 SHOP 유지. col 1만 EVENT로 변경.
> Floor 5는 전부 BATTLE이었으므로 col 1만 EVENT로 변경.

- [ ] **Step 3: GameManager.enter_node — EVENT 분기 추가**

```gdscript
MapNodeResource.RoomType.EVENT:
    change_state(GameState.EVENT)
    _request_scene("res://scenes/event/event_scene.tscn")
```

`GameState`에 EVENT는 이미 정의되어 있음. (`enum GameState { MAP, BATTLE, CARD_PICK, EVENT, SHOP, REST }`)

---

## Task 2: EventResource + 이벤트 데이터

- [ ] **Step 1: event_resource.gd 생성**

```gdscript
# resources/event_resource.gd
class_name EventResource
extends Resource

@export var event_name: String = ""
@export var description: String = ""
@export var choices: Array = []   # Array[EventChoiceResource]
```

```gdscript
# resources/event_choice_resource.gd
class_name EventChoiceResource
extends Resource

enum EffectType { NONE, GOLD, HEAL, DRAW_UP, REMOVE_CARD, ADD_RELIC, ADD_HERO }

@export var label: String = ""
@export var effect_type: EffectType = EffectType.NONE
@export var value: int = 0
@export var cost_gold: int = 0
@export var cost_hp: int = 0
```

- [ ] **Step 2: GameManager — 이벤트 풀 생성**

```gdscript
func _get_random_event() -> EventResource:
    var pool := _build_event_pool()
    return pool[randi() % pool.size()]

func _build_event_pool() -> Array:
    var EventRes = load("res://resources/event_resource.gd")
    var ChoiceRes = load("res://resources/event_choice_resource.gd")
    var events := []

    # 1. 황금 상자
    var e1 := EventRes.new(); e1.event_name = "황금 상자"
    e1.description = "낡은 황금 상자가 놓여 있다."
    var c1a := ChoiceRes.new(); c1a.label = "열기"; c1a.effect_type = ChoiceRes.EffectType.GOLD; c1a.value = 30
    var c1b := ChoiceRes.new(); c1b.label = "무시"; c1b.effect_type = ChoiceRes.EffectType.NONE
    e1.choices = [c1a, c1b]; events.append(e1)

    # 2. 상처 입은 전사
    var e2 := EventRes.new(); e2.event_name = "상처 입은 전사"
    e2.description = "부상당한 병사가 치료를 요청한다."
    var c2a := ChoiceRes.new(); c2a.label = "치료 (골드 -20)"; c2a.effect_type = ChoiceRes.EffectType.HEAL
    c2a.value = 15; c2a.cost_gold = 20
    var c2b := ChoiceRes.new(); c2b.label = "무시"; c2b.effect_type = ChoiceRes.EffectType.NONE
    e2.choices = [c2a, c2b]; events.append(e2)

    # 3. 고대 도서관
    var e3 := EventRes.new(); e3.event_name = "고대 도서관"
    e3.description = "신비로운 지식이 담긴 도서관. 공부하면 지식을 얻지만 기력을 소모한다."
    var c3a := ChoiceRes.new(); c3a.label = "공부 (HP -5)"; c3a.effect_type = ChoiceRes.EffectType.DRAW_UP
    c3a.value = 1; c3a.cost_hp = 5
    var c3b := ChoiceRes.new(); c3b.label = "무시"; c3b.effect_type = ChoiceRes.EffectType.NONE
    e3.choices = [c3a, c3b]; events.append(e3)

    # 4. 저주받은 제단
    var e4 := EventRes.new(); e4.event_name = "저주받은 제단"
    e4.description = "제단에 무언가를 바치면 강력한 유물을 얻을 수 있다."
    var c4a := ChoiceRes.new(); c4a.label = "카드 바치기 (덱에서 1장 제거)"; c4a.effect_type = ChoiceRes.EffectType.REMOVE_CARD
    c4a.value = 1
    var c4b := ChoiceRes.new(); c4b.label = "무시"; c4b.effect_type = ChoiceRes.EffectType.NONE
    e4.choices = [c4a, c4b]; events.append(e4)

    # 5. 동료 만남
    var e5 := EventRes.new(); e5.event_name = "동료 만남"
    e5.description = "역사 속 영웅이 합류를 요청한다."
    var c5a := ChoiceRes.new(); c5a.label = "합류시키기"; c5a.effect_type = ChoiceRes.EffectType.ADD_HERO
    var c5b := ChoiceRes.new(); c5b.label = "거절"; c5b.effect_type = ChoiceRes.EffectType.NONE
    e5.choices = [c5a, c5b]; events.append(e5)

    return events

# GameManager 상태에 추가
var pending_event: Resource = null  # EventResource

# enter_node의 EVENT 분기
MapNodeResource.RoomType.EVENT:
    pending_event = _get_random_event()
    change_state(GameState.EVENT)
    _request_scene("res://scenes/event/event_scene.tscn")
```

---

## Task 3: EventScene UI

- [ ] **Step 1: event_scene.tscn 생성**

```
Node2D (event_scene.tscn)
  ColorRect (bg, 전체화면 어두운 배경)
  VBoxContainer (center)
    Label (event_name_label)
    Label (event_desc_label)
    HBoxContainer (choices_container)
      # 버튼은 코드로 동적 생성
```

- [ ] **Step 2: event_scene.gd 작성**

```gdscript
# scenes/event/event_scene.gd
extends Node2D

@onready var name_label: Label = $VBoxContainer/EventNameLabel
@onready var desc_label: Label = $VBoxContainer/EventDescLabel
@onready var choices_container: HBoxContainer = $VBoxContainer/ChoicesContainer

func _ready() -> void:
    var event: Resource = GameManager.pending_event
    if event == null:
        GameManager._request_scene("res://scenes/map/map_scene.tscn")
        return
    name_label.text = event.event_name
    desc_label.text = event.description
    for choice in event.choices:
        var btn := Button.new()
        btn.text = choice.label
        btn.pressed.connect(_on_choice_selected.bind(choice))
        choices_container.add_child(btn)

func _on_choice_selected(choice: Resource) -> void:
    _apply_choice(choice)
    GameManager.pending_event = null
    GameManager._advance_nodes_from(GameManager.current_node_id)
    GameManager.change_state(GameManager.GameState.MAP)
    GameManager._request_scene("res://scenes/map/map_scene.tscn")

func _apply_choice(choice: Resource) -> void:
    match choice.effect_type:
        choice.EffectType.GOLD:
            GameManager.add_gold(choice.value)
        choice.EffectType.HEAL:
            if GameManager.spend_gold(choice.cost_gold):
                for hero in TeamManager.heroes:
                    TeamManager.heal(hero.hero_id, choice.value)
        choice.EffectType.DRAW_UP:
            if choice.cost_hp > 0:
                for hero in TeamManager.heroes:
                    TeamManager.take_damage(hero.hero_id, choice.cost_hp)
            DeckManager.base_draw_count += choice.value
        choice.EffectType.REMOVE_CARD:
            # 덱에서 랜덤 1장 제거 (간단 구현 — 나중에 선택 UI 추가)
            if not DeckManager.draw_pile.is_empty():
                DeckManager.draw_pile.remove_at(
                    randi() % DeckManager.draw_pile.size())
        choice.EffectType.ADD_HERO:
            # Plan 07 이후 구현 — 현재 텍스트 표시만
            print("동료 영입: Plan 07에서 구현 예정")
        choice.EffectType.NONE:
            pass
```

> `DeckManager.base_draw_count`는 `deck_manager.gd`에 `var base_draw_count: int = 5` 추가 후 `start_turn`에서 `draw_cards(base_draw_count)` 사용.

---

## Task 4: RestScene (기존 조용한 회복 → UI 선택)

- [ ] **Step 1: rest_scene.tscn + rest_scene.gd 생성**

```gdscript
# scenes/rest/rest_scene.gd
extends Node2D

func _ready() -> void:
    pass  # 버튼 클릭 대기

func _on_heal_pressed() -> void:
    for hero in TeamManager.heroes:
        TeamManager.heal(hero.hero_id, int(TeamManager.get_max_hp(hero.hero_id) * 0.3))
    _leave()

func _on_leave_pressed() -> void:
    _leave()

func _leave() -> void:
    GameManager._advance_nodes_from(GameManager.current_node_id)
    GameManager.change_state(GameManager.GameState.MAP)
    GameManager._request_scene("res://scenes/map/map_scene.tscn")
```

> 카드 강화는 Plan 07 이후 구현. 지금은 "HP 30% 회복" 또는 "그냥 나가기" 2가지.

- [ ] **Step 2: GameManager — REST enter_node 씬 전환 변경**

```gdscript
MapNodeResource.RoomType.REST:
    change_state(GameState.REST)
    _request_scene("res://scenes/rest/rest_scene.tscn")
    # _heal_all_heroes(10) 제거 — RestScene에서 처리
```

---

## Task 5: 테스트 + 커밋

- [ ] **Step 1: test_event.gd 작성**

```gdscript
# tests/test_event.gd
class_name TestEvent
extends RefCounted

const GameManagerClass = preload("res://autoload/game_manager.gd")

var passed: int = 0
var failed: int = 0

func run_all() -> Dictionary:
    test_event_pool_size()
    test_gold_event_exists()
    test_heal_event_costs_gold()
    test_draw_event_costs_hp()
    test_remove_card_event_exists()
    test_hero_recruit_event_exists()
    return {"passed": passed, "failed": failed}

func _assert(cond: bool, msg: String) -> void:
    if cond: passed += 1
    else: failed += 1; push_error("  FAIL: " + msg)
    if cond: print("  PASS: " + msg)

func _make_gm() -> GameManagerClass:
    var gm := GameManagerClass.new()
    return gm

func test_event_pool_size() -> void:
    print("[TestEvent] test_event_pool_size")
    var gm := _make_gm()
    var pool := gm._build_event_pool()
    _assert(pool.size() == 5, "이벤트 풀 5종")

func test_gold_event_exists() -> void:
    print("[TestEvent] test_gold_event_exists")
    var gm := _make_gm()
    var pool := gm._build_event_pool()
    var ChoiceRes = load("res://resources/event_choice_resource.gd")
    var found := false
    for ev in pool:
        for ch in ev.choices:
            if ch.effect_type == ChoiceRes.EffectType.GOLD and ch.value == 30:
                found = true
    _assert(found, "골드 +30 이벤트 존재")

func test_heal_event_costs_gold() -> void:
    print("[TestEvent] test_heal_event_costs_gold")
    var gm := _make_gm()
    var pool := gm._build_event_pool()
    var ChoiceRes = load("res://resources/event_choice_resource.gd")
    var found := false
    for ev in pool:
        for ch in ev.choices:
            if ch.effect_type == ChoiceRes.EffectType.HEAL and ch.cost_gold > 0:
                found = true
    _assert(found, "HP 회복 이벤트 골드 비용 있음")

func test_draw_event_costs_hp() -> void:
    print("[TestEvent] test_draw_event_costs_hp")
    var gm := _make_gm()
    var pool := gm._build_event_pool()
    var ChoiceRes = load("res://resources/event_choice_resource.gd")
    var found := false
    for ev in pool:
        for ch in ev.choices:
            if ch.effect_type == ChoiceRes.EffectType.DRAW_UP and ch.cost_hp > 0:
                found = true
    _assert(found, "드로우 증가 이벤트 HP 비용 있음")

func test_remove_card_event_exists() -> void:
    print("[TestEvent] test_remove_card_event_exists")
    var gm := _make_gm()
    var pool := gm._build_event_pool()
    var ChoiceRes = load("res://resources/event_choice_resource.gd")
    var found := false
    for ev in pool:
        for ch in ev.choices:
            if ch.effect_type == ChoiceRes.EffectType.REMOVE_CARD:
                found = true
    _assert(found, "카드 제거 이벤트 존재")

func test_hero_recruit_event_exists() -> void:
    print("[TestEvent] test_hero_recruit_event_exists")
    var gm := _make_gm()
    var pool := gm._build_event_pool()
    var ChoiceRes = load("res://resources/event_choice_resource.gd")
    var found := false
    for ev in pool:
        for ch in ev.choices:
            if ch.effect_type == ChoiceRes.EffectType.ADD_HERO:
                found = true
    _assert(found, "동료 영입 이벤트 존재")
```

- [ ] **Step 2: test_runner.gd에 TestEvent 추가**

- [ ] **Step 3: 전체 테스트 통과 확인**

예상: `=== Results: ~87 passed, 0 failed ===`

- [ ] **Step 4: 커밋**

```bash
git add resources/event_resource.gd resources/event_choice_resource.gd \
        autoload/game_manager.gd autoload/map_generator.gd \
        resources/map_node_resource.gd \
        scenes/event/ scenes/rest/ \
        tests/test_event.gd tests/test_runner.gd
git commit -m "feat: Plan 06 — 이벤트 룸 5종 + RestScene UI"
```
