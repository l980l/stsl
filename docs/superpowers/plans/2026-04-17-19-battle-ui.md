# Battle UI 개선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 전투 씬 UI를 4가지 측면에서 개선한다 — ① 적 클릭 영역 수정, ② 카드 효과 텍스트 표시, ③ 캐릭터 상태이상 아이콘(호버 툴팁), ④ 드래그 앤 드롭 카드 타겟 지정.

**Architecture:** 모든 변경은 `battle_scene.gd` 한 파일과 `battle_manager.gd`의 getter 2개 추가로 완결된다. 새 파일 없음. 드래그 앤 드롭은 Godot 내장 Control drag 시스템 대신 `_input` 기반 수동 구현으로 처리한다(별도 스크립트 파일 불필요).

**Tech Stack:** Godot 4.6 GDScript, Control nodes, Label.tooltip_text

---

## 파일 구조

| 파일 | 역할 | 변경 |
|------|------|------|
| `autoload/battle_manager.gd` | 상태이상 public getter 추가 | Modify |
| `scenes/battle/battle_scene.gd` | 나머지 4가지 기능 전부 | Modify |
| `tests/test_battle_manager.gd` | status getter 단위 테스트 | Modify |

---

### Task 1: BattleManager 상태이상 getter 추가 + 테스트

**Files:**
- Modify: `autoload/battle_manager.gd` (끝부분 get_enemy() 아래)
- Modify: `tests/test_battle_manager.gd`

- [ ] **Step 1: 테스트 작성 (실패 확인용)**

`tests/test_battle_manager.gd` 의 `run_all()` 반환 직전에 아래 두 함수 호출을 추가하고, 함수 본문도 추가한다.

```gdscript
# run_all() 안에 추가:
test_get_hero_status_empty_by_default()
test_get_enemy_status_after_apply()

# 파일 끝에 함수 추가:
func test_get_hero_status_empty_by_default() -> void:
    print("[TestBattleManager] test_get_hero_status_empty_by_default")
    var bm := BattleManagerClass.new()
    var status: Dictionary = bm.get_hero_status("napoleon")
    _assert(status.is_empty(), "영웅 상태 기본값 빈 딕셔너리")

func test_get_enemy_status_after_apply() -> void:
    print("[TestBattleManager] test_get_enemy_status_after_apply")
    var EnemyRes = load("res://resources/enemy_resource.gd")
    var bm := BattleManagerClass.new()
    var enemy = EnemyRes.new()
    enemy.enemy_name = "테스트적"
    enemy.max_hp = 30
    bm.setup_battle([enemy])
    bm._apply_status_to_enemy(0, "poison", 3)
    var status: Dictionary = bm.get_enemy_status(0)
    _assert(status.get("poison", 0) == 3, "적 상태 poison 3 조회")
    var empty: Dictionary = bm.get_enemy_status(99)
    _assert(empty.is_empty(), "범위 밖 인덱스 빈 딕셔너리")
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s tests/test_runner.gd 2>&1 | grep -E "FAIL|Results"
```

Expected: `get_hero_status` / `get_enemy_status` 관련 FAIL

- [ ] **Step 3: getter 구현**

`autoload/battle_manager.gd` 의 `get_enemy()` 함수 바로 아래에 추가:

```gdscript
func get_hero_status(hero_id: String) -> Dictionary:
    return _hero_status.get(hero_id, {}).duplicate()

func get_enemy_status(index: int) -> Dictionary:
    if index < 0 or index >= _enemy_status.size():
        return {}
    return _enemy_status[index].duplicate()
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s tests/test_runner.gd 2>&1 | tail -4
```

Expected: `=== Results: 338 passed, 0 failed ===`

- [ ] **Step 5: 커밋**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
git checkout -b feat/plan-19-battle-ui
git add autoload/battle_manager.gd tests/test_battle_manager.gd
git commit -m "feat: BattleManager 상태이상 public getter 추가"
```

---

### Task 2: 적 클릭 영역 수정

**Files:**
- Modify: `scenes/battle/battle_scene.gd` — `_make_enemy_slot()` 함수

현재 버튼: `position = Vector2(ENEMY_X + 20, y + 40)`, `size = Vector2(SLOT_W - 40, 160)` → 패널 가장자리에서 안쪽으로 20px 들여쓰기, 높이 160px. 패널 전체(360×280)를 커버하지 못해 클릭 누락 발생.

- [ ] **Step 1: `_make_enemy_slot` 수정**

`scenes/battle/battle_scene.gd` 의 `_make_enemy_slot()` 함수 내 btn 생성 부분을 다음으로 교체:

```gdscript
# 클릭 버튼 — 패널 전체를 커버 (텍스트 없음, flat)
var btn := Button.new()
btn.flat = true
btn.position = Vector2(ENEMY_X, y)
btn.size = Vector2(SLOT_W, SLOT_H)
btn.text = ""
var captured_index := index
btn.pressed.connect(func(): _on_enemy_pressed(captured_index))
add_child(btn)
```

intent_lbl, name_lbl, hp_lbl, block_lbl은 버튼 **이후에** add_child 되므로 Label이 시각적으로 위에 렌더링된다. Label의 기본 `mouse_filter = MOUSE_FILTER_IGNORE` 덕분에 입력은 버튼으로 통과된다.

- [ ] **Step 2: 에디터에서 확인**

게임 실행 → 전투 진입 → 적 슬롯 가장자리 클릭이 정상 동작하는지 확인.

- [ ] **Step 3: 커밋**

```bash
git add scenes/battle/battle_scene.gd
git commit -m "fix: 적 클릭 영역을 슬롯 전체로 확장"
```

---

### Task 3: 카드 효과 텍스트 표시

**Files:**
- Modify: `scenes/battle/battle_scene.gd` — `_refresh_hand()` + 헬퍼 함수 `_card_effect_text()` 추가

카드 버튼에 현재 `[cost]\nname\nowner_id` 만 표시됨. 효과(피해량, 방어, 상태이상 등)와 강화 표시를 추가한다.

- [ ] **Step 1: `_card_effect_text()` 헬퍼 추가**

`battle_scene.gd` 끝에 다음 함수를 추가한다:

```gdscript
func _card_effect_text(card: Resource) -> String:
    var lines: Array = []
    for eff in card.effects:
        match eff.effect_type:
            EffectRes.EffectType.DAMAGE:
                lines.append("피해 %d%s" % [eff.value, " (전체)" if eff.target == "ALL" else ""])
            EffectRes.EffectType.BLOCK:
                lines.append("방어 %d" % eff.value)
            EffectRes.EffectType.BLOCK_ALL:
                lines.append("전체 방어 %d" % eff.value)
            EffectRes.EffectType.FORMATION_BLOCK:
                lines.append("영웅수×%d 방어" % eff.value)
            EffectRes.EffectType.APPLY_STATUS:
                var st_name := {"poison":"독","weak":"약화","vulnerable":"취약",
                    "morale":"사기","charm":"매혹","strength":"강화","taunt":"도발"}.get(eff.status_type, eff.status_type)
                lines.append("%s %d" % [st_name, eff.value])
            EffectRes.EffectType.DRAW:
                lines.append("드로우 %d" % eff.value)
            EffectRes.EffectType.ENERGY:
                lines.append("에너지 +%d" % eff.value)
            EffectRes.EffectType.HEAL:
                lines.append("회복 %d" % eff.value)
            EffectRes.EffectType.HEAL_ALL:
                lines.append("전체 회복 %d" % eff.value)
            EffectRes.EffectType.GAIN_MORALE:
                lines.append("사기 +%d" % eff.value)
            EffectRes.EffectType.CONSUME_MORALE:
                lines.append("사기→피해 %d" % eff.bonus_value)
            EffectRes.EffectType.POISON_BURST:
                lines.append("독 즉발")
            EffectRes.EffectType.COUNTER_BLOCK:
                lines.append("방어도×%d%%" % eff.value)
            EffectRes.EffectType.COST_NEXT:
                lines.append("다음 비용 -%d" % eff.value)
            EffectRes.EffectType.CONDITIONAL_DMG:
                lines.append("%d/%d(%s)" % [eff.bonus_value, eff.value, eff.status_type])
            EffectRes.EffectType.SUMMON_TOKEN:
                lines.append("병사 소환")
            EffectRes.EffectType.CHARM:
                lines.append("매혹 %d" % eff.value)
    return "\n".join(lines)
```

- [ ] **Step 2: `_refresh_hand()` 의 카드 버튼 텍스트 수정**

`_refresh_hand()` 내 btn 생성 블록에서 `btn.text = ...` 줄을 다음으로 교체:

```gdscript
var card_name: String = card.get("card_name") if card.get("card_name") != null else "?"
var owner_id: String = card.get("owner_id") if card.get("owner_id") != null else ""
var upgraded_mark: String = " ★" if card.get("upgraded") else ""
var effect_desc: String = _card_effect_text(card)
btn.text = "[%d] %s%s\n%s\n%s" % [card.cost, card_name, upgraded_mark, owner_id, effect_desc]
btn.add_theme_font_size_override("font_size", 11)
```

- [ ] **Step 3: 에디터에서 확인**

게임 실행 → 전투 → 손패 카드에 "피해 6", "방어 5" 등 효과 텍스트가 표시되는지 확인.

- [ ] **Step 4: 커밋**

```bash
git add scenes/battle/battle_scene.gd
git commit -m "feat: 카드 버튼에 효과 텍스트 및 강화 표시 추가"
```

---

### Task 4: 상태이상 아이콘 (HP바 아래, 호버 툴팁)

**Files:**
- Modify: `scenes/battle/battle_scene.gd`

각 영웅/적 슬롯에 `HBoxContainer`를 추가해 상태이상을 아이콘+수치 Label로 표시한다. `Label.tooltip_text`를 사용해 마우스 호버 시 설명을 표시한다.

- [ ] **Step 1: 상수 및 변수 추가**

`battle_scene.gd` 상단 변수 선언부에 다음을 추가:

```gdscript
# 상태이상 표시 컨테이너 (hero_id → HBoxContainer, index → HBoxContainer)
var _hero_status_containers: Dictionary = {}
var _enemy_status_containers: Array = []

const STATUS_EMOJI := {
    "poison": "☠", "weak": "↓", "vulnerable": "⚡",
    "morale": "★", "charm": "♥", "strength": "↑",
    "taunt": "►", "counter_block": "🛡"
}
const STATUS_TOOLTIP := {
    "poison": "독: 턴 종료 시 N 피해. 매 턴 1 감소",
    "weak": "약화: 공격 피해 25% 감소. 매 턴 1 감소",
    "vulnerable": "취약: 받는 피해 50% 증가. 매 턴 1 감소",
    "morale": "사기: CONSUME_MORALE 카드로 추가 피해 제공",
    "charm": "매혹: 다음 행동 아군에게 적용",
    "strength": "강화: 피해 +N",
    "taunt": "도발: 이 대상이 우선 공격 받음",
    "counter_block": "반격 방어: 피해 = 현재 방어도 기반"
}
```

- [ ] **Step 2: `_make_hero_slot()` 에 상태 컨테이너 추가**

`_make_hero_slot()` 내 `return` 직전에 다음을 추가:

```gdscript
var status_box := HBoxContainer.new()
status_box.position = Vector2(HERO_X + 4, y + SLOT_H - 22)
status_box.size = Vector2(SLOT_W - 8, 20)
add_child(status_box)
# entry 딕셔너리에는 hero_id로 나중에 등록 (Task 4 Step 4에서)
return { "panel": panel, "name_lbl": name_lbl,
         "hp_lbl": hp_lbl, "block_lbl": block_lbl,
         "hero_id": "", "status_box": status_box }
```

- [ ] **Step 3: `_make_enemy_slot()` 에 상태 컨테이너 추가**

`_make_enemy_slot()` 내 `return` 직전에 다음을 추가:

```gdscript
var status_box := HBoxContainer.new()
status_box.position = Vector2(ENEMY_X + 4, y + SLOT_H - 22)
status_box.size = Vector2(SLOT_W - 8, 20)
add_child(status_box)
return { "panel": panel, "intent_lbl": intent_lbl, "btn": btn,
         "name_lbl": name_lbl, "hp_lbl": hp_lbl, "block_lbl": block_lbl,
         "status_box": status_box }
```

- [ ] **Step 4: `_setup_heroes()` 에서 status_containers 등록**

`_setup_heroes()` 내 `_update_hero_ui(hero.hero_id)` 줄 바로 위에 추가:

```gdscript
_hero_status_containers[hero.hero_id] = entry["status_box"]
```

`_setup_enemies()` 내 `_update_enemy_ui(i)` 줄 바로 위에 추가:

```gdscript
if i >= _enemy_status_containers.size():
    _enemy_status_containers.resize(i + 1)
_enemy_status_containers[i] = entry["status_box"]
```

- [ ] **Step 5: `_refresh_status_icons()` 헬퍼 추가**

파일 끝에 다음을 추가:

```gdscript
func _refresh_status_icons_hero(hero_id: String) -> void:
    var box: HBoxContainer = _hero_status_containers.get(hero_id)
    if box == null:
        return
    for child in box.get_children():
        child.queue_free()
    var status: Dictionary = BattleManager.get_hero_status(hero_id)
    for key in status:
        var val: int = status[key]
        if val <= 0:
            continue
        var lbl := Label.new()
        var emoji: String = STATUS_EMOJI.get(key, key)
        lbl.text = "%s%d" % [emoji, val]
        lbl.add_theme_font_size_override("font_size", 12)
        lbl.tooltip_text = STATUS_TOOLTIP.get(key, key).replace("N", str(val))
        lbl.mouse_filter = Control.MOUSE_FILTER_STOP
        box.add_child(lbl)

func _refresh_status_icons_enemy(index: int) -> void:
    if index >= _enemy_status_containers.size():
        return
    var box: HBoxContainer = _enemy_status_containers[index]
    if box == null:
        return
    for child in box.get_children():
        child.queue_free()
    var status: Dictionary = BattleManager.get_enemy_status(index)
    for key in status:
        var val: int = status[key]
        if val <= 0:
            continue
        var lbl := Label.new()
        var emoji: String = STATUS_EMOJI.get(key, key)
        lbl.text = "%s%d" % [emoji, val]
        lbl.add_theme_font_size_override("font_size", 12)
        lbl.tooltip_text = STATUS_TOOLTIP.get(key, key).replace("N", str(val))
        lbl.mouse_filter = Control.MOUSE_FILTER_STOP
        box.add_child(lbl)
```

- [ ] **Step 6: `_update_hero_ui()` / `_update_enemy_ui()` 에서 아이콘 갱신 호출**

`_update_hero_ui()` 함수 끝 `return` 직전에 추가:

```gdscript
_refresh_status_icons_hero(hero_id)
```

`_update_enemy_ui()` 함수 끝(마지막 if 블록 다음)에 추가:

```gdscript
_refresh_status_icons_enemy(index)
```

- [ ] **Step 7: `status_applied` 시그널 연결**

`_connect_signals()` 안에 추가:

```gdscript
BattleManager.status_applied.connect(_on_status_applied)
```

파일 끝에 핸들러 추가:

```gdscript
func _on_status_applied(target: String, _status_type: String, _stacks: int) -> void:
    if target.begins_with("enemy_"):
        var idx := target.substr(6).to_int()
        _refresh_status_icons_enemy(idx)
    else:
        _refresh_status_icons_hero(target)
```

- [ ] **Step 8: 에디터에서 확인**

게임 실행 → 전투 → 독/약화 카드 사용 → 적 슬롯 하단에 "☠3" 등 아이콘이 표시되고, 마우스 호버 시 툴팁이 나오는지 확인.

- [ ] **Step 9: 커밋**

```bash
git add scenes/battle/battle_scene.gd autoload/battle_manager.gd
git commit -m "feat: 캐릭터 슬롯에 상태이상 아이콘 및 호버 툴팁 추가"
```

---

### Task 5: 드래그 앤 드롭 카드 타겟 지정

**Files:**
- Modify: `scenes/battle/battle_scene.gd`

카드를 드래그해서 적 슬롯 위에서 드랍하면 해당 적에게 카드가 사용된다. 기존 클릭 방식도 유지된다. Godot `_input` 기반 수동 구현.

- [ ] **Step 1: 드래그 상태 변수 추가**

`battle_scene.gd` 상단 변수 선언부에 추가:

```gdscript
var _drag_card: Resource = null
var _drag_preview: Label = null
var _potential_drag_card: Resource = null
var _drag_start_pos: Vector2 = Vector2.ZERO
const DRAG_THRESHOLD := 10.0
```

- [ ] **Step 2: `_refresh_hand()` 에 button_down 연결 추가**

`_refresh_hand()` 내 `btn.pressed.connect(...)` 줄 **앞에** 추가:

```gdscript
var captured_card2 := card
btn.button_down.connect(func(): _on_card_button_down(captured_card2))
```

- [ ] **Step 3: `_on_card_pressed()` 드래그 중 클릭 무시**

`_on_card_pressed()` 맨 위에 추가:

```gdscript
if _drag_card != null:
    return  # 드래그 종료 후 발생하는 잔여 클릭 무시
```

- [ ] **Step 4: 드래그 핸들러 함수 추가**

파일 끝에 다음을 추가:

```gdscript
func _on_card_button_down(card: Resource) -> void:
    if not BattleManager.is_player_turn or not DeckManager.can_play(card):
        return
    _potential_drag_card = card
    _drag_start_pos = get_viewport().get_mouse_position()

func _input(event: InputEvent) -> void:
    if event is InputEventMouseMotion:
        if _potential_drag_card != null and Input.is_mouse_button_pressed(MOUSE_BUTTON_LEFT):
            var dist := get_viewport().get_mouse_position().distance_to(_drag_start_pos)
            if dist > DRAG_THRESHOLD and _drag_card == null:
                _start_drag(_potential_drag_card)
            if _drag_card != null:
                _drag_preview.position = get_viewport().get_mouse_position() + Vector2(8, 8)
    elif event is InputEventMouseButton \
            and event.button_index == MOUSE_BUTTON_LEFT \
            and not event.pressed:
        if _drag_card != null:
            _finish_drag(get_viewport().get_mouse_position())
        _potential_drag_card = null

func _start_drag(card: Resource) -> void:
    _drag_card = card
    _selected_card = null
    _message_label.text = "드래그하여 타겟 지정 ▶"
    # 미리보기 레이블
    _drag_preview = Label.new()
    _drag_preview.text = "[%d] %s" % [card.cost, card.get("card_name") if card.get("card_name") else "?"]
    _drag_preview.add_theme_font_size_override("font_size", 14)
    _drag_preview.size = Vector2(120, 30)
    _drag_preview.modulate = Color(1.0, 1.0, 0.6, 0.85)
    _drag_preview.z_index = 10
    add_child(_drag_preview)
    # 유효 드롭 타겟 강조 (살아있는 적 패널)
    for i in range(_enemy_nodes.size()):
        if _enemy_nodes[i]["panel"].visible and BattleManager.is_enemy_alive(i):
            _enemy_nodes[i]["panel"].color = Color(0.35, 0.12, 0.12)

func _finish_drag(drop_pos: Vector2) -> void:
    var needs_target := false
    for effect in _drag_card.effects:
        match effect.effect_type:
            EffectRes.EffectType.DAMAGE:
                if effect.target == "SINGLE":
                    needs_target = true
            EffectRes.EffectType.APPLY_STATUS:
                if effect.target == "SINGLE":
                    needs_target = true
            EffectRes.EffectType.COUNTER_BLOCK, \
            EffectRes.EffectType.CONSUME_MORALE, \
            EffectRes.EffectType.POISON_BURST, \
            EffectRes.EffectType.CONDITIONAL_DMG:
                needs_target = true
        if needs_target:
            break

    if needs_target:
        for i in range(_enemy_nodes.size()):
            var panel: ColorRect = _enemy_nodes[i]["panel"]
            if not panel.visible or not BattleManager.is_enemy_alive(i):
                continue
            if panel.get_global_rect().has_point(drop_pos):
                BattleManager.play_card(_drag_card, i)
                _cleanup_drag()
                return
        # 유효 타겟에 드롭하지 않은 경우: 취소
        _cleanup_drag()
    else:
        # 타겟 불필요 카드: 아무 곳에 드롭해도 즉시 사용
        BattleManager.play_card(_drag_card, -1)
        _cleanup_drag()

func _cleanup_drag() -> void:
    if _drag_preview != null:
        _drag_preview.queue_free()
        _drag_preview = null
    _drag_card = null
    _selected_card = null
    _message_label.text = ""
    for entry in _enemy_nodes:
        if entry["panel"].visible:
            entry["panel"].color = Color(0.18, 0.10, 0.10)
```

- [ ] **Step 5: 에디터에서 확인**

게임 실행 → 전투 → 스트라이크 카드를 드래그 → 적 패널 위에서 드롭 → 카드가 사용되는지 확인. 다음도 확인:
- 타겟 필요 카드를 빈 공간에 드롭: 취소됨 (카드 그대로)
- 타겟 불필요 카드(블록 등)를 드래그 후 아무데나 드롭: 즉시 사용됨
- 클릭 방식도 여전히 동작하는지 확인

- [ ] **Step 6: 커밋**

```bash
git add scenes/battle/battle_scene.gd
git commit -m "feat: 드래그 앤 드롭 카드 타겟 지정 구현"
```

---

### Task 6: 최종 검증 및 PR

- [ ] **Step 1: 전체 테스트 통과 확인**

```bash
cd "H:/AIRelatedDefaultPath/ClaudeCode_Base/GameProjects/CardGameProject/stsl"
"H:/Program Files/Godot/Godot_v4.6.2-stable_win64_console.exe" --headless -s tests/test_runner.gd 2>&1 | tail -4
```

Expected: `=== Results: 338 passed, 0 failed ===`

- [ ] **Step 2: 에디터 통합 확인 체크리스트**

에디터에서 게임 실행 후 순서대로 확인:
1. 적 슬롯 전체 영역 클릭 → 타겟 선택됨
2. 손패 카드에 효과 텍스트 표시 (피해량, 방어값 등)
3. 독/약화 카드 사용 → 적/영웅 슬롯 하단에 아이콘 등장
4. 상태이상 아이콘 호버 → 툴팁 표시
5. 카드 드래그 → 적 위에서 드롭 → 카드 사용됨
6. 타겟 필요 카드를 빈 공간에 드롭 → 취소됨

- [ ] **Step 3: PR 생성**

```bash
git push -u origin feat/plan-19-battle-ui
gh pr create --title "feat: Plan 19 — 전투 UI 개선 (클릭영역·카드텍스트·상태아이콘·드래그앤드롭)" \
  --body "$(cat <<'EOF'
## Summary
- 적 클릭 영역을 슬롯 전체(360×280)로 확장
- 카드 버튼에 효과 텍스트(피해/방어/상태이상 수치) 및 강화(★) 표시
- 캐릭터 슬롯 하단에 상태이상 아이콘+수치, 호버 시 한국어 툴팁
- `_input` 기반 드래그 앤 드롭: 카드를 드래그해 적 위에서 드롭하면 해당 적에게 사용
- BattleManager에 `get_hero_status()` / `get_enemy_status()` getter 추가

## Test Plan
- [ ] `=== Results: 338 passed, 0 failed ===` 확인
- [ ] 에디터에서 위 6가지 시나리오 직접 확인
EOF
)"
```
