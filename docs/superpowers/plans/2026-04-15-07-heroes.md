# Plan 07: 클레오파트라 + 이순신 + 나폴레옹 완성

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MVP 3인 영웅 시스템을 완성한다. 캐릭터당 아키타입 1개, 카드 15~18종. 각 캐릭터 고유 메카닉(사기/독/방어반격) 구현. 동료 영입 이벤트 실제 동작.

**아키타입 방향:**
- 나폴레옹 — **돌격(Blitz)**: 저비용 카드 연타 → 사기(Morale) 축적 → 폭발 피해
- 클레오파트라 — **독살(Venom)**: 독 스택 누적 → 독 폭발로 대량 피해
- 이순신 — **거북선(Turtle)**: 방어도 누적 → 방어도 비례 반격 피해

**Architecture:**
- 고유 메카닉은 EffectResource 새 타입으로 처리 (GAIN_MORALE, CONSUME_MORALE, POISON_BURST, COUNTER_BLOCK)
- 스타터 카드 2종 × 각 캐릭터 / 보상 풀 카드 13~16종 × 각 캐릭터
- TeamManager 다중 영웅 지원 확인 (기존 코드 이미 지원)
- 동료 영입: Plan 06 스텁 → 실제 구현

**Tech Stack:** Godot 4.6, GDScript, 헤드리스 테스트 (기존 ~87개 + 신규 ~12개 → ~99개)

---

## 브랜치

```bash
git checkout -b feat/plan-07-heroes
```

---

## 카드 설계

### 카드 구조 원칙
- **스타터 카드**: 런 시작 시 덱에 들어오는 카드. 보상 풀과 별개.
- **보상 풀 카드**: 전투 후 보상으로 선택 가능한 카드 종류.
- 같은 종류를 덱에 여러 장 넣을 수 있음 (STS 방식).

---

### 나폴레옹 (Blitz 아키타입) — 총 16종

**스타터 2종** (런 시작 덱: 스트라이크×3 + 디펜드×2)

| 이름 | 비용 | 효과 |
|------|------|------|
| 스트라이크 | 1 | DAMAGE 6 |
| 디펜드 | 1 | BLOCK 5 |

**보상 풀 14종**

| 이름 | 비용 | 효과 | 아키타입 역할 |
|------|------|------|--------------|
| 돌격 | 1 | DAMAGE 8 + 사기+1 | 핵심 — 사기 축적 |
| 연속 타격 | 1 | DAMAGE 4, 플레이 후 에너지+1 | 핵심 — 연타 엔진 |
| 황제의 명령 | 2 | DAMAGE 5 (ALL) + 사기+2 | 핵심 — 사기 대량 축적 |
| 사기 폭발 | 0 | 사기 3 소모 → DAMAGE 20 | 핵심 — 사기 소모 피날레 |
| 돌격 명령 | 1 | 사기+2, 이번 턴 다음 카드 비용-1 | 보조 — 사기 + 콤보 |
| 쾌속 전진 | 1 | DAMAGE 3 × 사기 스택 수 | 보조 — 사기 스케일 |
| 군기 확립 | 1 | DRAW 2 + 사기+1 | 보조 — 드로우 + 사기 |
| 포격 | 2 | DAMAGE 14 (사기 2 이상이면 DAMAGE 20) | 보조 — 사기 조건 강타 |
| 헤비 스트라이크 | 2 | DAMAGE 14 | 범용 — 고비용 강타 |
| 클리브 | 1 | DAMAGE 5 (ALL) | 범용 — 광역 |
| 아이언 웨이브 | 1 | DAMAGE 5 + BLOCK 5 | 범용 — 공수 |
| 아이언 디펜스 | 1 | BLOCK 10 | 범용 — 방어 |
| 포이즌 스트라이크 | 1 | DAMAGE 3 + 독 3 | 범용 — 상태이상 |
| 전열 정비 | 0 | DRAW 1, 이번 턴에만 | 유틸 — 0코스트 드로우 |

---

### 클레오파트라 (Venom 아키타입) — 총 16종

**스타터 2종** (런 시작 덱: 독침×3 + 왕실 방어×2)

| 이름 | 비용 | 효과 |
|------|------|------|
| 독침 | 1 | DAMAGE 3 + 독 3 |
| 왕실 방어 | 1 | BLOCK 6 |

**보상 풀 14종**

| 이름 | 비용 | 효과 | 아키타입 역할 |
|------|------|------|--------------|
| 독안개 | 2 | 독 3 (ALL) | 핵심 — 광역 독 |
| 독구름 | 1 | DAMAGE 2 (ALL) + 독 2 (ALL) | 핵심 — 광역 독+피해 |
| 뱀의 독 | 1 | 독 8 | 핵심 — 독 집중 |
| 독 폭발 | 1 | 대상 현재 독 스택만큼 즉시 피해 (독 스택 초기화) | 핵심 — 독 폭발 |
| 이중 독 | 1 | 독 있는 적에게 DAMAGE 8, 없으면 DAMAGE 2 | 보조 — 독 조건 강타 |
| 치명독 | 2 | DAMAGE 4 + 독 5 + vulnerable 1 | 보조 — 독 + 취약 |
| 독 강화 | 0 | 이번 턴 내 독 카드 독 스택 +2 | 보조 — 독 증폭 |
| 독 회복 | 1 | 전체 적의 독 스택 합계 × 1만큼 HP 회복 | 보조 — 독 기반 힐 |
| 유혹 | 1 | 매혹 2 (적 2턴 행동 건너뜀) | 보조 — 매혹 |
| 저주의 시선 | 1 | weak 2 + vulnerable 2 | 범용 — 디버프 |
| 파라오의 명 | 1 | BLOCK 8 + DRAW 2 | 범용 — 방어+드로우 |
| 독 방패 | 1 | BLOCK 4, 독 있는 적에게 BLOCK 10 | 범용 — 독 조건 방어 |
| 왕실 칙령 | 2 | 덱에서 독 카드 2장 즉시 드로우 | 유틸 — 서치 |
| 재생독 | 1 | DAMAGE 3 + 독 1 (ALL) | 유틸 — 저비용 광역 |

---

### 이순신 (Turtle 아키타입) — 총 16종

**스타터 2종** (런 시작 덱: 방패×3 + 역공×2)

| 이름 | 비용 | 효과 |
|------|------|------|
| 방패 | 1 | BLOCK 7 |
| 역공 | 1 | BLOCK 3 + DAMAGE 3 |

**보상 풀 14종**

| 이름 | 비용 | 효과 | 아키타입 역할 |
|------|------|------|--------------|
| 거북선 돌격 | 2 | DAMAGE 6 + 현재 방어도의 60%만큼 추가 피해 | 핵심 — 방어도 반격 |
| 반격 | 1 | DAMAGE = 현재 방어도 수치 | 핵심 — 방어도→피해 |
| 철갑 | 1 | BLOCK 14 | 핵심 — 방어도 대량 축적 |
| 학익진 | 2 | BLOCK 5 (팀 전체) | 핵심 — 광역 방어 |
| 배수진 | 1 | HP 8 지불 → BLOCK 18 | 보조 — 고효율 방어 |
| 노량 해전 | 2 | DAMAGE 24, 방어도 8 이상일 때만 사용 가능 | 보조 — 방어도 조건 강타 |
| 진형 강화 | 1 | 생존 영웅 수 × 5만큼 BLOCK | 보조 — 팀 규모 스케일 |
| 수군 훈련 | 1 | BLOCK 5 + DRAW 1 | 보조 — 방어+드로우 |
| 한산대첩 | 3 | DAMAGE 8 (ALL) + BLOCK 8 (팀 전체) | 범용 — 광역 공수 |
| 불굴 | 2 | 팀 전체 HP 12% 회복 | 범용 — 팀 힐 |
| 엄정한 훈련 | 1 | DRAW 2 + BLOCK 4 | 범용 — 드로우+방어 |
| 거북선 방패 | 1 | BLOCK 8 + 이번 턴 다음 피해 50% 감소 | 범용 — 강화 방어 |
| 필사즉생 | 1 | DAMAGE = (최대HP - 현재HP) × 0.5 | 유틸 — 피해 기반 역전 |
| 해군 기동 | 0 | BLOCK 3, 이번 턴에만 | 유틸 — 0코스트 방어 |

---

## 파일 구조

```
resources/
  effect_resource.gd     ← (수정) 신규 EffectType 추가
autoload/
  game_manager.gd        ← (수정) 3캐릭터 카드 풀 + 영입 로직
  battle_manager.gd      ← (수정) 신규 효과 처리
  team_manager.gd        ← (수정) increase_max_hp (릴릭 연동 대비)
characters/
  heroes/
    cleopatra/cleopatra.tscn  ← (수정) ColorRect 황금색
    yi_sun_sin/yi_sun_sin.tscn ← (수정) ColorRect 청록색
scenes/
  battle/battle_scene.gd ← (수정) 다중 영웅 UI
  event/event_scene.gd   ← (수정) ADD_HERO 실제 구현
tests/
  test_heroes.gd         ← (신규)
  test_runner.gd         ← (수정)
```

---

## Task 1: EffectResource 신규 EffectType 추가

- [ ] **Step 1: effect_resource.gd 수정**

```gdscript
enum EffectType {
    DAMAGE, BLOCK, APPLY_STATUS, DRAW, ENERGY, HEAL,
    GAIN_MORALE,       # 사기 +N
    CONSUME_MORALE,    # 사기 N 소모 → DAMAGE bonus_value. value=소모량, bonus_value=피해
    POISON_BURST,      # 대상 독 스택 즉시 피해 + 독 초기화
    COUNTER_BLOCK,     # DAMAGE = 현재 방어도 × (value / 100). value=비율(%)
    BLOCK_ALL,         # 팀 전체 BLOCK value
    HEAL_ALL,          # 팀 전체 HP value% 회복
    FORMATION_BLOCK,   # 생존 영웅 수 × value BLOCK
    COST_NEXT,         # 이번 턴 다음 카드 비용 -value (ENERGY와 구분)
    CONDITIONAL_DMG,   # 조건 충족 시 bonus_value, 아니면 value 피해. status_type=조건키
}

@export var bonus_value: int = 0   # 조건부/추가 효과에 사용
```

---

## Task 2: BattleManager — 신규 효과 처리

- [ ] **Step 1: _apply_card_effects에 신규 케이스 추가**

```gdscript
EffectRes.EffectType.GAIN_MORALE:
    var cur: int = _hero_status.get(card.owner_id, {}).get("morale", 0)
    _hero_status[card.owner_id] = _hero_status.get(card.owner_id, {})
    _hero_status[card.owner_id]["morale"] = cur + effect.value
    status_applied.emit(card.owner_id, "morale", effect.value)

EffectRes.EffectType.CONSUME_MORALE:
    var morale: int = _hero_status.get(card.owner_id, {}).get("morale", 0)
    if morale >= effect.value:
        _hero_status[card.owner_id]["morale"] = morale - effect.value
        if target_enemy_index >= 0 and target_enemy_index < _enemies.size():
            _deal_damage_to_enemy(target_enemy_index, effect.bonus_value)

EffectRes.EffectType.POISON_BURST:
    if target_enemy_index >= 0 and target_enemy_index < _enemies.size():
        var poison: int = _enemy_status[target_enemy_index].get("poison", 0)
        if poison > 0:
            _deal_damage_to_enemy(target_enemy_index, poison)
            _enemy_status[target_enemy_index]["poison"] = 0

EffectRes.EffectType.COUNTER_BLOCK:
    # DAMAGE = 현재 방어도 × (effect.value / 100)
    var block: int = _hero_block.get(card.owner_id, 0)
    var dmg: int = int(block * effect.value / 100.0)
    if target_enemy_index >= 0 and dmg > 0:
        _deal_damage_to_enemy(target_enemy_index, dmg)

EffectRes.EffectType.BLOCK_ALL:
    if team_mgr:
        for hero in team_mgr.heroes:
            _hero_block[hero.hero_id] = _hero_block.get(hero.hero_id, 0) + effect.value

EffectRes.EffectType.HEAL_ALL:
    if team_mgr:
        for hero in team_mgr.heroes:
            team_mgr.heal(hero.hero_id, effect.value)

EffectRes.EffectType.FORMATION_BLOCK:
    if team_mgr:
        var count: int = team_mgr.get_living_heroes().size()
        _hero_block[card.owner_id] = _hero_block.get(card.owner_id, 0) + count * effect.value

EffectRes.EffectType.CONDITIONAL_DMG:
    # status_type 조건으로 target의 상태 확인
    if target_enemy_index >= 0:
        var has_status: bool = _enemy_status[target_enemy_index].get(effect.status_type, 0) > 0
        var dmg: int = effect.bonus_value if has_status else effect.value
        _deal_damage_to_enemy(target_enemy_index, dmg)
```

---

## Task 3: GameManager — 3캐릭터 카드 풀

- [ ] **Step 1: _napoleon_card_pool() 확장 (14장)**

기존 5장 + 9장 추가. 위 카드 표 기준으로 전부 구현.

- [ ] **Step 2: _cleopatra_card_pool() 신규 (14장)**

```gdscript
func _cleopatra_card_pool() -> Array:
    var CardRes = load("res://resources/card_resource.gd")
    var EffRes = load("res://resources/effect_resource.gd")
    var cards := []
    # 독안개 — 독 3 (ALL)
    # 독구름 — DAMAGE 2 ALL + APPLY_STATUS(poison 2, ALL)
    # 뱀의 독 — APPLY_STATUS(poison 8)
    # 독 폭발 — POISON_BURST
    # 이중 독 — CONDITIONAL_DMG(value=2, bonus_value=8, status_type="poison")
    # 치명독 — DAMAGE 4 + APPLY_STATUS(poison 5) + APPLY_STATUS(vulnerable 1)
    # 독 강화 — ENERGY +2 (독 전용 에너지 대신 간략화: DRAW 1 + 다음 독 카드 효과 증폭은 후속 작업)
    # 독 회복 — HEAL (독 스택 합계 기반 — 간략화: HEAL 6)
    # 유혹 — APPLY_STATUS(charm 2)
    # 저주의 시선 — APPLY_STATUS(weak 2) + APPLY_STATUS(vulnerable 2)
    # 파라오의 명 — BLOCK 8 + DRAW 2
    # 독 방패 — CONDITIONAL_DMG 대신 BLOCK (status 조건 방어: 간략화 BLOCK 10)
    # 왕실 칙령 — DRAW 2 (서치 간략화)
    # 재생독 — DAMAGE 3 + APPLY_STATUS(poison 1, ALL)
    return cards
```

> **구현 원칙:** 조건부 효과가 복잡한 카드(독 강화, 독 방패 등)는 1단계 간략화 버전으로 먼저 구현. 폴리싱 단계에서 정확한 조건 효과 추가.

- [ ] **Step 3: _yi_sun_sin_card_pool() 신규 (14장)**

거북선 돌격(COUNTER_BLOCK 60%), 반격(COUNTER_BLOCK 100%), 철갑(BLOCK 14), 학익진(BLOCK_ALL 5), 배수진(HEAL -8 + BLOCK 18), 노량해전(DAMAGE 24 조건부), 진형 강화(FORMATION_BLOCK 5), 수군 훈련(BLOCK 5 + DRAW 1), 한산대첩(DAMAGE 8 ALL + BLOCK_ALL 8), 불굴(HEAL_ALL 12), 엄정한 훈련(DRAW 2 + BLOCK 4), 거북선 방패(BLOCK 8), 필사즉생(조건부 피해), 해군 기동(BLOCK 3).

> 배수진의 HP 지불은 HEAL -8 (음수 힐 = 피해)로 처리. `team_mgr.take_damage(card.owner_id, 8)` 직접 호출.

---

## Task 4: 캐릭터 씬 색상 수정

- [ ] **Step 1: cleopatra.tscn ColorRect 색상 → Color(1, 0.8, 0) (황금)**
- [ ] **Step 2: yi_sun_sin.tscn ColorRect 색상 → Color(0, 0.6, 0.8) (청록)**

---

## Task 5: 동료 영입 이벤트 실제 구현

- [ ] **Step 1: GameManager._recruit_hero_pool() 추가**

```gdscript
func _recruit_hero_pool() -> Array:
    var existing := []
    for h in TeamManager.heroes:
        existing.append(h.hero_id)
    var pool := []
    if "cleopatra" not in existing:
        pool.append(_make_cleopatra_hero())
    if "yi_sun_sin" not in existing:
        pool.append(_make_yi_sun_sin_hero())
    return pool

func _make_cleopatra_hero() -> Resource:
    var HeroRes = load("res://resources/hero_resource.gd")
    var hero := HeroRes.new()
    hero.hero_id = "cleopatra"
    hero.hero_name = "클레오파트라"
    hero.max_hp = 60
    hero.character_scene = load("res://characters/heroes/cleopatra/cleopatra.tscn")
    return hero

func _make_yi_sun_sin_hero() -> Resource:
    var HeroRes = load("res://resources/hero_resource.gd")
    var hero := HeroRes.new()
    hero.hero_id = "yi_sun_sin"
    hero.hero_name = "이순신"
    hero.max_hp = 75
    hero.character_scene = load("res://characters/heroes/yi_sun_sin/yi_sun_sin.tscn")
    return hero

func _add_initial_deck_for(hero: Resource) -> void:
    var CardRes = load("res://resources/card_resource.gd")
    var EffRes = load("res://resources/effect_resource.gd")
    match hero.hero_id:
        "cleopatra":
            # 독침×2 + 왕실 방어×2
            for _i in range(2):
                var c := CardRes.new(); c.card_name = "독침"
                c.owner_id = "cleopatra"; c.cost = 1; c.play_animation = "attack"
                var e := EffRes.new(); e.effect_type = EffRes.EffectType.DAMAGE
                e.value = 3; e.target = "SINGLE"
                var ep := EffRes.new(); ep.effect_type = EffRes.EffectType.APPLY_STATUS
                ep.status_type = "poison"; ep.value = 3
                c.effects = [e, ep]; DeckManager.add_card_to_deck(c)
            for _i in range(2):
                var c := CardRes.new(); c.card_name = "왕실 방어"
                c.owner_id = "cleopatra"; c.cost = 1; c.play_animation = "idle"
                var e := EffRes.new(); e.effect_type = EffRes.EffectType.BLOCK; e.value = 6
                c.effects = [e]; DeckManager.add_card_to_deck(c)
        "yi_sun_sin":
            # 방패×2 + 역공×2
            for _i in range(2):
                var c := CardRes.new(); c.card_name = "방패"
                c.owner_id = "yi_sun_sin"; c.cost = 1; c.play_animation = "idle"
                var e := EffRes.new(); e.effect_type = EffRes.EffectType.BLOCK; e.value = 7
                c.effects = [e]; DeckManager.add_card_to_deck(c)
            for _i in range(2):
                var c := CardRes.new(); c.card_name = "역공"
                c.owner_id = "yi_sun_sin"; c.cost = 1; c.play_animation = "attack"
                var eb := EffRes.new(); eb.effect_type = EffRes.EffectType.BLOCK; eb.value = 3
                var ed := EffRes.new(); ed.effect_type = EffRes.EffectType.DAMAGE
                ed.value = 3; ed.target = "SINGLE"
                c.effects = [eb, ed]; DeckManager.add_card_to_deck(c)
```

- [ ] **Step 2: EventScene._apply_choice ADD_HERO 케이스 구현**

```gdscript
choice.EffectType.ADD_HERO:
    var pool := GameManager._recruit_hero_pool()
    if pool.is_empty():
        return  # 이미 모든 영웅 영입
    var hero: Resource = pool[randi() % pool.size()]
    TeamManager.add_hero(hero)
    GameManager._add_initial_deck_for(hero)
```

---

## Task 6: BattleScene 다중 영웅 UI

- [ ] **Step 1: BattleScene — 영웅 영역을 TeamManager.heroes 기반으로 동적 생성**

현재 나폴레옹 고정 → heroes 루프로 최대 3명 표시. 영웅 수에 따라 x 좌표 균등 배치.

```gdscript
# _setup_heroes() 수정
func _setup_heroes() -> void:
    var positions := [Vector2(300, 500), Vector2(500, 500), Vector2(700, 500)]
    for i in range(TeamManager.heroes.size()):
        var hero := TeamManager.heroes[i]
        var placeholder := _spawn_character(hero.character_scene, positions[i])
        hero_nodes[hero.hero_id] = placeholder
        _create_hero_hpbar(hero, positions[i] + Vector2(-50, 80))
```

---

## Task 7: 카드 보상 개선 — 영웅별 카드 보상

현재 `_generate_card_rewards()`가 나폴레옹 풀만 사용. 팀에 있는 영웅들의 카드 풀을 합쳐서 3장 랜덤 선택.

- [ ] **Step 1: GameManager._generate_card_rewards() 수정**

```gdscript
func _generate_card_rewards() -> Array:
    var pool := []
    for hero in TeamManager.heroes:
        match hero.hero_id:
            "napoleon":   pool.append_array(_napoleon_card_pool())
            "cleopatra":  pool.append_array(_cleopatra_card_pool())
            "yi_sun_sin": pool.append_array(_yi_sun_sin_card_pool())
    pool.shuffle()
    return pool.slice(0, min(3, pool.size()))
```

---

## Task 8: 테스트 + 커밋

- [ ] **Step 1: test_heroes.gd 작성 (~12개)**

```gdscript
# tests/test_heroes.gd
class_name TestHeroes
extends RefCounted
# napoleon_card_pool size == 14
# cleopatra_card_pool size == 14
# yi_sun_sin_card_pool size == 14
# GAIN_MORALE 효과 적용
# CONSUME_MORALE 사기 부족 시 발동 안 함
# POISON_BURST 독 있는 적에게 피해
# COUNTER_BLOCK 방어도 비례 피해
# BLOCK_ALL 팀 전체 방어도
# recruit_hero_pool 나폴레옹만 있을 때 2명 반환
# recruit_hero_pool 풀 파티일 때 0명 반환
# _add_initial_deck_for cleopatra 덱 4장 추가
# _generate_card_rewards 다중 영웅 시 카드 풀 합산
```

- [ ] **Step 2: test_runner.gd에 TestHeroes 추가**

- [ ] **Step 3: 전체 테스트 통과**

예상: `=== Results: ~99 passed, 0 failed ===`

- [ ] **Step 4: 커밋**

```bash
git commit -m "feat: Plan 07 — 3인 영웅 시스템 완성 (나폴레옹 16종, 클레오파트라 16종, 이순신 16종)"
```
